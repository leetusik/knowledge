# bootstrap_agentic_workspace.sh

The agentic-workspace bootstrap project. One explainer so far: how the
`/explain` skill's document API works, and the fallback path it takes when
the API is unreachable.
