# Result — P17.S2: Reconcile the old explain-skill copies

Status returned: **`needs_operator`** (repo-side reconciliation complete; the user-level
`~/.claude` copy is an operator-machine touch — staged command below).

## What changed

1. **Deleted the project copy.** Removed `.claude/skills/explain/SKILL.md` and its now-empty
   `.claude/skills/explain/` directory (the third in-repo duplicate that caused the double
   `explain` listing in this repo's sessions). Deleted via file `rm`/`rmdir`, not git — the
   orchestrator stages the deletion at commit.
   - **Settings grep (reported, not edited):** `.claude/settings.json` has no match. One
     match in `.claude/settings.local.json:12` — `"Read(//Users/sugang/.claude/skills/explain/**)"`.
     That is a read-permission for the **user-level** path `~/.claude/skills/explain/**` (the
     copy that is *kept* and operator-updated), **not** the deleted project path
     `.claude/skills/explain/`. So there is no dangling reference to the deleted path. Per
     plan, I did not edit any settings file.

2. **Derived the portable `.agents` copy.** Rewrote `.agents/skills/explain/SKILL.md` as a
   structural derivation of the canonical `plugin/skills/explain/SKILL.md` (v2):
   - Frontmatter: `name: explain` + the v2 `description` **only** (byte-same description
     value as canonical — verified equal). No `argument-hint`, no `allowed-tools` — the
     portable variant's shape.
   - Body (everything after the frontmatter): **byte-identical** to the canonical body
     (§1–§8, including the byte-identical §2 resolver). Generated by extracting the
     canonical body programmatically, so no re-derivation/rewording. Verified: stripped-body
     byte-compare plugin ↔ `.agents` is empty (25009-byte body on both sides).

3. **Refreshed `.agents/skills/explain/agents/openai.yaml`.** Updated `short_description` to
   the v2 description (byte-same as canonical, escaped-inner-`"` double-quoted YAML matching
   the sibling `design-cowork` pattern; parses back byte-for-byte to the canonical
   description). Kept `display_name`, `default_prompt`, and `policy` (`allow_implicit_invocation: true`).
   - **openai.yaml tools-field finding:** I checked the sibling `.agents/skills/*/agents/openai.yaml`
     files (`design-cowork`, `do-next-slice`, `commit`, and the whole set). **Every one carries
     only `interface` (`display_name` / `short_description` / `default_prompt`) + `policy`
     (`allow_implicit_invocation`) — there is no tools/permissions/allowed-tools field anywhere
     in the schema.** So the v2 tool needs (WebSearch/WebFetch + `git diff`/`log`/`show` reads)
     **cannot be expressed in this yaml.** Left `policy` untouched, as the plan directs. (For
     Codex/OpenAI the SKILL body's prose is the tool guidance; the Claude-plugin `allowed-tools`
     surface lives only on the canonical `plugin/` copy.)

4. **Added the sync guard.** New `scripts/skills_parity.py` (root-only, never shipped —
   same stance as `plugin_parity.py`; 74 lines incl. docstring, stdlib only, exit 0/1):
   strips the leading `---` frontmatter block from the canonical and `.agents` copies,
   byte-compares the bodies (mismatch = FAIL), compares the two `description:` frontmatter
   values (mismatch = WARN, still exit 0 unless the body also mismatched), and FAILs with a
   clear message if either file is missing. Wired into `.github/workflows/plugin-ci.yml` as
   one added step (`- run: python3 scripts/skills_parity.py`) directly after the existing
   `plugin_parity.py` step; nothing else in the workflow touched. No `plugin/**` change in
   this slice ⇒ no plugin version bump.

### Minor deviation (hardening, within plan intent)

The missing-file FAIL message originally used `path.relative_to(REPO_ROOT)`, which raises
`ValueError` for a path outside the repo. In production both compared paths are always under
`REPO_ROOT`, but I wrapped the display in a `try/except ValueError` fallback so the guard
always emits a clean "FAIL — explain skill copy missing" line instead of a traceback,
regardless of path. This strengthens the plan's "FAIL with a clear message if either file is
missing" requirement; no behavior change to the pass/body/description paths.

## Validation (all run this session)

| Check | Command | Outcome |
|---|---|---|
| Positive | `python3 scripts/skills_parity.py` | `PASS … in body parity` — **exit 0** |
| Negative (body) | module import, `PORTABLE` → perturbed `/tmp` copy (`…+ "X"`) | `FAIL … bodies drifted` — **exit 1** |
| Negative (missing) | module import, `PORTABLE` → non-existent path | `FAIL … copy missing` — **exit 1** |
| Body identity | stripped-body byte-compare plugin ↔ `.agents` | empty diff (identical) |
| Description identity | `description:` value plugin ↔ `.agents` | byte-same |
| Project copy gone | `test -e .claude/skills/explain` | gone |
| State integrity | `python3 scripts/workflow.py validate` | `Workflow validation passed.` — **exit 0** |

The negative checks used a temp copy pointed at through the imported module — the real tree
was never perturbed (positive run still PASSes after). Per plan, `scripts/plugin_parity.py`
was **not** run as a gate (known red until S4).

## Operator gate (how the slice finishes)

The user-level `~/.claude/skills/explain/SKILL.md` still holds the OLD (pre-v2) content and
must be updated to the v2 canonical body, but `~/.claude` is an operator-machine path — never
written from the repo. Repo-side reconciliation is complete and validated. **Operator must run:**

```
cp plugin/skills/explain/SKILL.md ~/.claude/skills/explain/SKILL.md
```

then the orchestrator verifies with:

```
diff ~/.claude/skills/explain/SKILL.md plugin/skills/explain/SKILL.md
```

(empty diff = success) and finishes the slice.

**Dupe resolution decided (operator-ratified in plan):** the project copy is **deleted**; the
user-level copy is **kept and updated** to v2; `plugin/` + `.agents/` are the two **shipped**
copies, now guarded by `skills_parity.py` in CI against future drift.

## Files changed

- `.claude/skills/explain/SKILL.md` — **deleted** (+ removed empty dir)
- `.agents/skills/explain/SKILL.md` — rewritten (portable v2 derivation)
- `.agents/skills/explain/agents/openai.yaml` — `short_description` → v2
- `scripts/skills_parity.py` — new (root-only drift guard)
- `.github/workflows/plugin-ci.yml` — one added CI step
