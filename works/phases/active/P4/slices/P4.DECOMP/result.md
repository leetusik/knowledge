# Result

- Phase ID: P4
- Slice ID: P4.DECOMP
- Slice: decompose phase "Knowledge feature core improvements"
- Review status: pending
- Next action: orchestrator promotes D1 into `P4.S5` (publish hygiene), then executes slices by order (S1 → S2 → S3 → S4 → S5 → REVIEW).

## Outcome

Spot-verified the pre-gathered audit against the actual code (all findings confirmed — see `phase.md` Findings & Notes) and decomposed P4 into **five middle slices**. Created four bare-folder slices (S1–S4); the fifth (publish hygiene) is **proposed only** in `phase.md` for the orchestrator to create via `promote-deferred D1`. Seeded `phase.md` with Context, Decomposition (breakdown + rationale + ordering), the verified Findings & Notes, Constraints, and a started Doc impact list.

Slices created (bare folders, no `plan.md`):

| Slice | Name | Kind | Risk | Order | Depends |
|---|---|---|---|---|---|
| `P4.S1` | Search quality — CJK-capable FTS tokenization, recency ranking, pagination | implementation | medium | 1 | — |
| `P4.S2` | API completeness — DELETE document, `GET /api/tags`, `GET /api/projects` | implementation | medium | 2 | — |
| `P4.S3` | Reindex robustness — incremental single-path reindex + startup drift self-heal | implementation | low | 3 | P4.S1 |
| `P4.S4` | Cross-link convention — related-docs metadata, API exposure, backfill | implementation | medium | 4 | — |

Proposed (NOT created — orchestrator creates via D1 promotion):

- `P4.S5` — **Publish hygiene** — publish-safe `source` metadata (drop absolute local paths, backfill 6 docs, sanitize at server write path) + hide `docs/versions/` from the built site via mkdocs `exclude_docs` (never `nav:`/`strict:`) + README/config touch-ups. kind: implementation, risk: low, order: 5, advisory depends_on: P4.S4. Create with:
  `python3 scripts/workflow.py promote-deferred D1 --phase P4 --slice P4.S5 --name "Publish hygiene — publish-safe source metadata + hide docs/versions from the built site"` (then set `--order 5`, `--risk low`, `--kind implementation`, `--depends-on P4.S4` as the engine allows).

Rationale for the breakdown, ordering, and risk levels is recorded in `phase.md` → Decomposition.

## Deviations from Plan

None. Followed the plan's proposed sketch: created S1–S4, left publish hygiene as a `phase.md` proposal for D1 promotion. Slice names are the plan's areas, lightly expanded for clarity; risks match the sketch (S1/S2/S4 medium, S3 low; proposed S5 low). Added an advisory `depends_on P4.S1` to S3 (settle the FTS schema before the reindex refactor) and proposed `depends_on P4.S4` for S5 (both backfill the same 6 docs' frontmatter); `depends_on` is advisory only.

## Validation Run

- `python3 scripts/workflow.py new-slice --phase P4 --slice P4.S1..S4 ...` — all four created OK.
- `python3 scripts/workflow.py validate` — **passed** ("Workflow validation passed.").
- Verified each new slice folder is bare (only the engine's `slice.json` + scaffolded `result.md`; no `plan.md`) and carries the intended kind/risk/order/depends_on.

## Files Changed

- Created: `works/phases/active/P4/slices/P4.S1/` (bare), `.../P4.S2/`, `.../P4.S3/`, `.../P4.S4/` (via `new-slice`; each contains engine-scaffolded `slice.json` + `result.md` only)
- Edited: `works/phases/active/P4/phase.md` (seeded Context, Decomposition, Findings & Notes, Constraints, Doc impact)
- Edited: `works/phases/active/P4/slices/P4.DECOMP/result.md` (this file)
- Also regenerated by `new-slice`: `works/state.json`, `works/backlog.md`, `works/index.json`, `works/phases/active/P4/phase.json`

## Doc Versions Created

- None (decomposition slice — durable docs are versioned only at `P4.REVIEW`). Anticipated per-area doc targets seeded in `phase.md` → Doc impact.

## Roadmap Updates

- P4 middle slices now exist: S1–S4 created; S5 (publish hygiene) pending D1 promotion by the orchestrator.

## Retrospective

- The audit was accurate. One nuance worth flagging for S1: the FTS schema is `CREATE ... IF NOT EXISTS`, so a tokenizer change will NOT take effect on an existing DB — S1 must add an explicit `documents_fts` drop/rebuild path (reindex rebuilds `documents` but not the FTS table today).
- Publish hygiene and cross-link both backfill the same 6 docs' frontmatter — kept S5 after S4 to avoid churn; noted the server-side sanitize approach so publish-safety survives without a skill change (skill is frozen until P7).
