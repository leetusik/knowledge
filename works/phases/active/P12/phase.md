# Phase P12: Web App: Tenant Dashboard & Project Detail Pages

_Intent: see [intent.md](intent.md)._

## Objective

Authenticated web app with a tenant dashboard and project detail pages consuming the tenancy and usage APIs; all web UI features (knowledge graph included) free

## Context

P12 is knowledge's **first authenticated-frontend phase** — a greenfield browser surface for signed-in tenants. It builds the tenant dashboard + project detail pages (and per-tenant read surfaces) as a **new Next.js app in a `web/` subdir of this repo**, modeled on `~/projects/personal/hi2vi_web` (stack + patterns + design gate) and — far more directly — on **vocky's already-built P4** (`~/projects/personal/vocky/web/`), which stood up the exact same phase against the same accounts/tenancy/usage API shape (knowledge's control plane was itself ported from vocky). "hi2vi_web-style" = stack + patterns + design gate, **not** repo topology.

The backend is a **pure JSON API** (FastAPI, single uvicorn worker + in-process `WRITE_LOCK`) with **bearer-only auth** (opaque session token returned in the JSON body, 30-day TTL, sha256-hex at rest) and **no CORS / cookies / CSRF / session middleware — by design**. The dashboard is a **session-token client** of `/auth/*` + `/app/*` only — never the `vk_`-keyed `/api/*` machine surface (from the app's own auth flow). The no-CORS wall is sidestepped with a **server-side BFF**: the browser talks only to the Next origin; Next calls `knowledge-api` server-to-server with `Authorization: Bearer`; the backend token is sealed into an httpOnly cookie. No backend CORS change is needed.

**Knowledge-specific divergence from vocky (the load-bearing planning fact):** vocky's read surfaces already lived on `/app/*` (e.g. `/app/feedback`), so its web app needed **no** backend change. Knowledge's read surfaces do **not**: **documents live only on `/api/*`** (metered, `vk_`/session-bearer, scoped by project *name* string) and **the graph is a build-time mkdocs asset over `docs/` only** (`scripts/graph_hook.py` → `graph.json`, tenant #1 only). So **P12's read slices (S5, S6) most likely ADD small session-scoped, tenant-scoped, *unmetered* `/app/*` read routes** to the backend (documents list/search; per-tenant graph data) rather than reuse the metered `/api/*`. That is a legitimate, isolated backend addition — it does **not** touch the D-P12-2 "no CORS" principle. The exact mechanism is each read slice's own planning-turn decision; DECOMP only records it as an open question.

## Decomposition

Foundation chain **S1 → S2**, then a **fan-out** of four surfaces (**S3, S4, S5, S6**) that each need only the S2 shell + authed client and are otherwise independent (any order after S2). One `high` slice (the auth/BFF security boundary); the rest `medium`; none `low` (nothing is fully mechanical). Mirrors the house foundation-then-fan-out pattern with the one security-sensitive slice at the top tier.

- **P12.S1 — App scaffold + design-system foundation** · `medium` · order 1
  Stand up the Next.js 16 / React 19 / TS app in `web/` (pnpm, Tailwind v4 CSS-first `@theme`, ESLint/Prettier, `next.config.ts` `output: "standalone"`, runnable via `pnpm dev`). Port the design system — **most directly from vocky's `web/`** (already adapted from hi2vi_web for a dashboard), hi2vi_web as upstream: the `@theme` token system (colors/fonts/type scale/radius; **no dark-mode toggle** — explicit surface tokens), `cn()` (`clsx` + **extended `tailwind-merge`** registering the custom `text-*` scale — load-bearing), self-hosted `next/font` (Noto Sans KR + JetBrains Mono), the CVA primitive set (`button`, `card`, `section`, `field`, `badge`, `grid`) **plus the `data-table` primitive** (lists of projects/credentials/documents need it), and the copy-as-data `src/content/` convention. Scaffold `web/design/canvas/` (tokens mirror + a couple section cards + `_ds_manifest.json`) for the **deferred-to-P14** design gate. **Neutral placeholder palette** (token *names* kept swappable). Purely presentational — no backend calls yet.
  _Rationale:_ load-bearing foundation every page builds on; templated but needs adaptation → `medium`, not `low`.

- **P12.S2 — Auth + BFF proxy + authenticated app shell** · `high` · order 2 · depends P12.S1
  The security-sensitive core and the whole server-side data layer. Typed **server-side knowledge API client** (server-only base-URL env, `getJson`/`sendJson`, `ApiError`, bearer injection, **every call `cache: "no-store"`** — per-user data must never hit Next's fetch cache; normalize the signup-`tenant`(singular)/login-`tenants[]`(plural) asymmetry to `tenants[0]`, solo-owner MVP). **BFF auth flow**: signup + login pages → Next server calls knowledge `/auth/signup|login` → seal the returned token into an **AES-256-GCM httpOnly `SameSite=Strict` cookie** (key `sha256(SESSION_SECRET)`, `{token, exp}`, 30-day TTL matching the backend, `Secure` in prod) the Next app sets for itself, never exposed to browser JS → server guards (a `requireIdentity` analog verifying live via `GET /auth/me`, wrapped in React `cache()`; + same-origin check) gating `/app` routes → logout clears cookie + calls `/auth/logout` → redirect-to-`/login` on backend 401/expiry (the `(auth)` bounce **re-verifies against `/auth/me`** to avoid a revoked-cookie ping-pong). One audited BFF pipeline for the public mutations (415 → 403 same-origin → 429 per-IP → 400/422 zod → backend → seal → `{ok}`; backend `detail` never echoed, preserving enumeration-safety; login throttled stricter than signup). **Authenticated app shell** (sticky topbar: brand · tenant/workspace · user email · logout; over `[rail | main]`) that S3–S6 render inside; rail links to not-yet-shipped routes render as muted "Soon", never 404.
  _Rationale:_ auth boundary + sealed-cookie sealing + BFF token wiring = the riskiest slice → top tier.

- **P12.S3 — Tenant dashboard: projects + create + tenant usage** · `medium` · order 3 · depends P12.S2
  Post-login home. Projects list + create-project (`GET|POST /app/projects`); tenant usage summary (`GET /app/usage?days=30`: `totals{total, documents_created, documents_deleted, searches}` + zero-filled daily trend + project list). Reuse the vocky **usage components** (`stat-tiles`, dependency-free inline-SVG `trend-chart` — `maxTotal` floored at 1, empty-series + all-zero handling, `aria-label`/sr-only summary) generic over knowledge's metric-counts shape. Server components via S2's authed client; create-project is a **server action** (+ `useActionState` island + `revalidatePath`). Tenant name comes from the S2 shell's cached `/auth/me` (no extra `GET /app/tenant` round-trip). Render the project list from `/app/projects` (canonical), not `usage.projects` (narrower serializer).
  _Rationale:_ read + one create + derived-metrics rendering → `medium`.

- **P12.S4 — Project detail: info + credentials + project usage** · `medium` · order 4 · depends P12.S2
  Per-project page. Project info (`GET /app/projects/{id}`); **credentials management** — `POST` mint with a **show-once `vk_` key** modal (returned once at mint; never re-render/log it), `GET` list metadata (incl. revoked; never the hash), `DELETE` soft-revoke (`/app/projects/{id}/credentials[/{cid}]`); project usage (`GET /app/projects/{id}/usage?days=30`, surfacing each key's `last_used_at`). Adds the project→detail link to the dashboard's project rows. **404-never-403** on foreign/cross-tenant ids (backend-enforced). Authed mutations are **server actions** (call `requireIdentity()` **outside** the action's `try` — it `redirect()`s by throwing; a `"use server"` file exports **only** async functions).
  _Rationale:_ CRUD + credential mint/revoke UX; boundary backend-enforced → `medium` (may bump to `high` at its planning turn if show-once credential handling proves riskier).

- **P12.S5 — Per-tenant documents browse + search** · `medium` · order 5 · depends P12.S2
  The per-tenant knowledge viewer (vocky's flagship-read-surface analog). Per-project document list + a document detail/read view + search, scoped to the tenant/project. **Key open question resolved at this slice's planning turn:** documents are **not** on `/app/*` today — either (preferred) **add small session-scoped, tenant-scoped, *unmetered* `/app` read routes** to the backend (e.g. `GET /app/documents`, `GET /app/documents/{id}`, `GET /app/search`) so web-UI browsing does **not** pollute the metered usage counts, **or** call the existing **metered** `/api/documents` + `/api/search` with the session bearer (tenant mode → `tenants[0]`), accepting web-UI searches inflate the `searches` metric. Also handle the control-plane `project` UUID ↔ content-plane `project` *name* bridge (documents filter by name string). **Read + search only.**
  _Rationale:_ read + search rendering + a likely small backend read-route addition → `medium` (may bump to `high` if backend routes/search prove nontrivial).

- **P12.S6 — Knowledge graph in the web app (per-tenant)** · `medium` · order 6 · depends P12.S2
  Move the graph into the app as a per-tenant (optionally per-project) view. Two parts, both designed at this slice's planning turn: (1) a **per-tenant graph-data source** — today `scripts/graph_hook.py` emits `graph.json` from `docs/` frontmatter (`related:` edges + `tags`) at mkdocs build, **tenant #1 only**; per-tenant data must come from the content plane, most likely a **new session-scoped `/app` graph endpoint** (e.g. `GET /app/graph` / `GET /app/projects/{id}/graph`) reusing the hook's inversion logic over a tenant's documents; and (2) **porting the ~1130-line zero-dependency `<canvas>` force-sim renderer** (`docs/javascripts/graph.js`) into a React client component wired through the BFF. **This is the slice most likely to bump to `high` at its planning turn** — a novel canvas-in-React port + a graph-data path that does not exist yet — but it is a port of proven code with a clear boundary, so DECOMP rates it `medium` and preserves the bump-up option.
  _Rationale:_ substantial but bounded port of existing, working graph code + a new per-tenant data route → `medium`, the top bump-to-`high` candidate.

**Why 6 (above the 2–4 house range):** standing up a whole app is inherently large; vocky's equivalent was 5 ("top of the range but justified for a whole app"). P12 = vocky's 5 surfaces (scaffold / auth+BFF+shell / dashboard / project-detail / read-surface) **plus** the net-new **graph** the operator explicitly moved into the app. S1/S2 split by both risk and concern (design system vs. auth security). S3/S4/S5/S6 are four distinct surfaces with distinct API sets — merging any pair makes an oversized slice. The 6-slice shape is deliberate.

## Findings & Notes

### Decisions

- **D-P12-1 — App location.** The app is a **subdirectory `web/` in this repo**, versioned/committed by the existing workflow engine. "hi2vi_web-style" = stack/patterns/design-gate, not repo topology.
- **D-P12-2 — Browser↔API boundary.** A **Next.js server-side BFF proxy**. Next calls `knowledge-api` server-to-server with `Authorization: Bearer`; the browser only ever talks to the Next origin; the backend session token lives in a **sealed AES-256-GCM httpOnly cookie**, never in browser JS; **no web-side DB, no backend CORS change.** (Adding session-scoped `/app` *read* routes for documents/graph in S5/S6 is orthogonal to this — it extends the control-plane API, not the CORS/origin model.)
- **D-P12-3 — Design gate + deploy.** P12 **ports** the design system + builds **functional** pages against a **neutral placeholder palette** (names swappable), scaffolds `web/design/canvas/`, but **defers the intensive Claude design-gate session AND production deploy to P14**. S1 sets up only local-dev-runnable + `output: "standalone"` (mirrors vocky: build in P4, deploy in P6).

**Operator scope decisions (confirmed at this decomposition):**
- **Per-tenant documents browse + search → IN P12** (mirrors vocky's flagship read surface). The authenticated web app becomes the **per-tenant knowledge viewer**; the public mkdocs site stays tenant #1's public surface. Per-tenant mkdocs sites do not exist today (other tenants' content is gitignored and in no build), so this is the "coexist-vs-replace the viewer" answer: per-tenant browsing lives in the app.
- **Knowledge graph → MOVE INTO the web app** (per-tenant/per-project graph view), porting the existing canvas renderer + a new per-tenant graph-data path.
- **Production deploy → DEFERRED to P14** (the landing-page / Claude-design-gate phase). P12 builds functional pages + `output: "standalone"`, runnable via `pnpm dev` only; Dockerfile / compose / edge vhost land with the design gate in P14.

### Implementation anchors (verified against the current tree at DECOMP)

Verified `2026-07-16`: all endpoint shapes, the documents-not-on-`/app/*` fact, the graph asset paths, and the vocky/hi2vi_web template paths match the plan's research exactly — **no drift**. Details below.

- **Knowledge session API — the surfaces the app consumes** (`server/`):
  - `server/auth_api.py` — `POST /auth/signup` → 201 `{token, user, tenant}` (singular); `POST /auth/login` → 200 `{token, user, tenants:[…]}` (plural, generic 401); `POST /auth/logout` → 204 (bearer optional); `GET /auth/me` → `{user, tenants:[…]}`. Opaque `secrets.token_urlsafe(32)` (`generate_opaque_token`), 30-day `SESSION_TTL`, token in JSON body, sha256-hex at rest.
  - `server/app_api.py` — `GET /app/tenant`; `GET|POST /app/projects` (POST `{name}` → 201 `{project}`); `GET /app/projects/{id}` (404 cross-tenant, `_load_scoped_project`); `POST /app/projects/{id}/credentials` (`{name?}` → 201 `{credential, key:"vk_…"}`, key once); `GET /app/projects/{id}/credentials` (incl. revoked, never hash); `DELETE …/credentials/{cid}` → 204 soft-revoke. Serializers `serialize_project`/`serialize_credential`.
  - `server/usage_api.py` — `GET /app/usage?days=1..365` (default 30) → `{window, totals{total, documents_created, documents_deleted, searches}, daily_counts[](zero-filled), projects}`; `GET /app/projects/{id}/usage?days=…` → same + `project` + `credentials`.
  - Session guard `server/accounts/auth.py::require_user` (bearer → sha256 → active session → user → `tenants[0]`; generic 401; solo-owner MVP). Two-mode `/api/*` resolver `server/api_auth.py` (a session token resolves to `tenants[0]` in tenant mode — relevant only if S5 uses the metered `/api/*`).
  - **NOT on `/app/*` today (S5/S6 add read routes):** documents and search live only on `/api/*` — `GET /api/documents`, `GET /api/documents/{id}`, `GET /api/documents/by-path/{path}`, `GET /api/search`, `POST/DELETE /api/documents` (all in `server/main.py`, metered, scoped by project *name*); and per-tenant graph data (only `scripts/graph_hook.py` build-time over `docs/`). Confirmed: no `/app/documents`, `/app/search`, or `/app/graph` route exists.
- **Graph assets to port (S6):** `scripts/graph_hook.py` (7.7 KB, PyYAML-only `related`+`tags` inversion → `graph.json`; must not import `server/*`), `docs/javascripts/graph.js` (**1130 lines**, zero-dep `<canvas>` force sim — confirmed line count), `docs/graph.md` (`.kb-graph` mount), styling in `docs/stylesheets/extra.css` §10.
- **vocky `web/` — the near-verbatim template** (`~/projects/personal/vocky/web/src/`, all paths confirmed present): `lib/session.ts` (sealed AES-256-GCM cookie), `lib/bff.ts` (audited auth pipeline), `lib/auth-guards.ts` (`requireIdentity` + `cache()`), `lib/vocky/{client,auth,app,types}.ts` (typed API-client seam; swap base-URL env + `/auth`+`/app` response types), `components/ui/data-table.tsx`, `components/usage/{stat-tiles,trend-chart}.tsx`, `components/app-shell/*`, `app/(auth)/*`, `app/(app)/*`, `app/api/auth/*/route.ts`. Two server-only env keys → for knowledge: `KB_API_BASE_URL` + `SESSION_SECRET` (neither `NEXT_PUBLIC_`, lazy-read so `next build` needs no env). Its P4 record: `~/projects/personal/vocky/works/phases/active/P4/phase.md`.
- **hi2vi_web upstream** (`~/projects/personal/hi2vi_web`, confirmed present): `src/app/globals.css` (`@theme` tokens), `src/components/ui/*` (CVA primitives), `src/lib/utils.ts` (`cn` + extended tailwind-merge), `next.config.ts`/`Dockerfile`/`compose.prod.yml`/`deploy/edge/*.conf` (the **P14** standalone-Docker-behind-shared-`edge` deploy reference).

### Cross-slice notes (DECOMP)

- All six middle slices are **bare folders** (only `slice.json`); each slice's `plan.md` is written by the orchestrator at that slice's own turn. S5 and S6 each carry a real open question (backend read-route mechanism) that must be **resolved and written into their own `plan.md`** at planning time — DECOMP deliberately did not decide it.
- Risk is the phase's main cost lever: S2 is `high` (auth/BFF security boundary); S1/S3/S4/S5/S6 are `medium`. **None are `low`** — nothing here is fully mechanical (even S1's port needs adaptation of the token system + tailwind-merge registration). S4 and especially S6 carry an explicit **bump-to-`high`** option at their planning turns.
- `web/` does not exist yet; DECOMP wrote **no** app code. Docs are untouched (no new `docs/versions/*`).

### Cross-slice notes (S1 — App scaffold + design-system foundation)

- **D-P12-3 REVISION (operator directive at S1's turn).** At S1 the operator directed **adopting hi2vi_web's real design system now** — knowledge's actual palette/type/fonts/component styling — and building **whole, polished pages modeled on hi2vi_web**, dropping the DECOMP framing of a "neutral placeholder palette + deferred Claude design gate". The heavy design gate is **no longer deferred for the app's own look**; only **production deploy** stays P14 (P14 remains the separate public landing-page / product-webpage phase). P12.REVIEW should consolidate this into `decisions.md` (supersedes the "neutral placeholder" half of D-P12-3; the "deploy in P14" half stands).
- **Design system is now the ADOPTED hi2vi_web brand**, not a placeholder: bright signal-green `#2ff28f` / links `#00b66a` on near-black-green `#061512` + a dark-green forest ramp, warm terracotta/amber signals, greenish light surfaces. Token **names** + non-color scales come from hi2vi_web via vocky's dashboard extraction; the color/font **values** are hi2vi_web's real ones. Every S2–S6 page inherits this — build "whole polished pages", not neutral stubs.
- **Two-source split (settled, unambiguous):** vocky = STRUCTURE (file layout, config, `cn()` architecture, copy-as-data `content/`, net-new `data-table.tsx`, `design/canvas/` shape); hi2vi_web = DESIGN (colors, fonts, type-scale weights, primitive hardcoded rgba). Where they differ, hi2vi values win, vocky's scaffold shape wins. S2–S6 should port **new** components (usage `stat-tiles`/`trend-chart`, `app-shell`, auth pages, etc.) from vocky's structure but retune any hardcoded neutral value to the hi2vi tokens — everything token-based already renders in the real green automatically.
- **Fonts:** 3 base faces self-hosted via `next/font/local` — **FULL** `PretendardVariable.woff2` (not hi2vi's marketing `.subset`, since knowledge renders unbounded operator/agent Korean) → `--font-pretendard`/`font-sans`; Inter → `--font-inter`/`font-en`; JetBrains → `--font-jetbrains`/`font-mono`. hi2vi's 4 hero-clipping display faces were dropped (marketing-only). Vendored in `web/public/fonts/`.
- **Load-bearing:** `src/lib/utils.ts` `cn()` registers the 14 custom `text-*` sizes in tailwind-merge's `font-size` group — keep it in sync with the `--text-*` tokens in `globals.css` or a size + color in one `cn()` call silently drops the size.
- **S1 boundary held:** purely presentational — **zero** backend/`fetch`/`server-only`/auth imports in `web/src/` (grep-verified); `/` prerenders **○ Static**. `app: pnpm dev` on `127.0.0.1:3030`; `next.config.ts` has `output: "standalone"`. Config deps are S1-only (no `server-only`/`zod`/`vitest`; no `test` script).
- **Gotchas for S2:**
  - **`src/app/page.tsx` is an S1 preview** exercising the primitives — **S2 replaces it** with the root auth redirect (signed-in → `/dashboard`, else → `/login`), matching vocky's `app/page.tsx`. Do not preserve the preview.
  - **Env + `lib/` rename deferred to S2 (per S1 plan):** add the two server-only env keys `KB_API_BASE_URL` + `SESSION_SECRET` (neither `NEXT_PUBLIC_`, lazy-read so `next build` needs no env), `.env.example`, and create `lib/knowledge/{client,auth,app,types}.ts` (vocky's `lib/vocky/*` renamed). S1 deliberately shipped none of `lib/{session,bff,auth-guards,env,client-ip,rate-limit}.ts`.
  - **S2 must add its deps** to `package.json` — `server-only`, `zod` (+ `vitest` if it adds tests) — which S1 intentionally excluded.
  - `content/index.ts` currently exports **only** the S1 symbols (SECTION_IDS/SITE/NAV_LINKS/SKIP_TO_CONTENT + types). S2+ add `auth`/`app`/`dashboard`/`project` content modules and extend the barrel.
  - `content/section-ids.ts` holds only preview ids (`overview`, `primitives`) — replace/extend with real app section ids.
  - `layout.tsx` is `lang="ko"` (Korean-first, matches hi2vi_web); the authenticated app shell wraps the `(app)` route group, not this root layout.

### Doc impact (S1)

- `frontend.md` — the ported/adopted design system now lives in `web/`: Tailwind v4 CSS-first `@theme` tokens (the real hi2vi_web green palette), `cn()` with extended tailwind-merge (custom `text-*` scale), self-hosted `next/font/local` (full Pretendard + Inter + JetBrains), the CVA primitive set **+ the net-new `data-table`**, copy-as-data `src/content/`, and `web/design/canvas/` (adopted-token mirror for the P14 gate). App is `output: "standalone"`, runnable via `pnpm dev` on `:3030`.
- `decisions.md` — **D-P12-3 revised** (see the S1 cross-slice note above): adopt hi2vi_web's real design system now (whole polished pages); only production deploy stays deferred to P14.
- `architecture.md` — knowledge gains a new `web/` Next.js app subdir (the authenticated browser surface); S1 is presentational-only (no BFF/auth yet — that is S2).
- `operations.md` — web-app local build/run stub: `pnpm install` / `pnpm dev` (`:3030`) / `pnpm build` (`output: "standalone"`); full production deploy = P14.

### Cross-slice notes (S2 — Auth + BFF proxy + authenticated app shell)

**The BFF / sealed-cookie auth model, AS BUILT.** The browser talks only to the Next origin; Next calls `knowledge-api` server-to-server with `Authorization: Bearer`; the backend token is sealed into an httpOnly cookie. Constants that S3–S6 depend on:
- Cookie **`knowledge_session`**, **AES-256-GCM**, key `sha256(SESSION_SECRET)`, IV `randomBytes(12)`, 16-byte tag, envelope `v1.<iv>.<ct>.<tag>` base64url, payload `{token, exp}`, TTL 30d (`Max-Age=2592000`), attrs `httpOnly / sameSite=strict / secure(prod-only) / path=/`. `openSession` → `null` on any failure, never throws.
- **Two server-only env keys**: `KB_API_BASE_URL` (local default `http://127.0.0.1:8766`) + `SESSION_SECRET`. Both read lazily via `src/lib/env.ts` (`import "server-only"`); neither `NEXT_PUBLIC_`; `next build` needs no env. `.env.example` written.
- **Client seam = `src/lib/knowledge/{client,auth,types}.ts`** (was vocky's `lib/vocky/*`). `client.ts` (`getJson`/`getRaw`/`sendJson`, `ApiError{status,detail}`, bearer injection, **`cache:"no-store"` on every call**) is the ONE place Next talks to the backend. **S3–S6 add `src/lib/knowledge/app.ts`** (the `/app/*` calls) + extend `types.ts` with the `/app/*` shapes — the `app.ts` file and S3+ types were deliberately NOT ported in S2.
- **Guards** (`src/lib/auth-guards.ts`): `requireIdentity = cache(async …)` (`requireSession` → live `GET /auth/me`; catches **only `ApiError` 401 → `redirect("/login")`, rethrows everything else**). The `(app)` layout awaits it; any `(app)` page may also call it and share the single `/auth/me` (React `cache()`). `redirectIfAuthenticated` re-verifies `/auth/me` to break the revoked-cookie ping-pong. The BFF pipeline (`src/lib/bff.ts`) is `415→403→429(before parse)→400→422→502/500→200`, body always `{ok}`, backend `detail` never echoed. Routes at `/api/auth/{login,signup,logout}` (`nodejs` + `force-dynamic`).

**App design language established (adopted hi2vi_web, real).**
- **Auth pages = the dark "secure threshold" gate** (`operator/login`): `bg-ink` stage + decorative glow/grid layers (`(auth)/layout.tsx`), forest-gradient card + Secure pill + trust-chip footer (`auth-card.tsx`), dark translucent inputs + full-width green submit + badge error (`credentials-form.tsx`).
- **App shell = the light "workspace" console** (`operator/(console)`): sticky white topbar (brand + tenant breadcrumb + email + logout) over `[248px rail | main]`; rail = white `bg-canvas`, mono-uppercase eyebrow, active row `border-green-deep bg-surface-green` + `aria-current`, not-yet-shipped routes as muted **"Soon"** pills (never a 404 link).
- **Two button languages, kept distinct:** the flat app-chrome `AppButton`/`appButtonClass` + `Tag` chip (`components/ui/app-button.tsx`: `rounded-md`, primary `bg-green hover:bg-green-deep`, secondary/ghost/danger) is what the shell + S3–S6 app pages use. The marketing pill `Button` (rounded-full, hover-translate, green glow) **stays reserved for P14's public landing** — do not use it inside the app. Green = primary/active/focus only.
- `BRAND` (`content/site.ts`) = wordmark with the leading **`k`** as the green mark (knowledge has no digit; `text-green` on the dark gate, `text-green-deep` on light). `StatusPill` deliberately not introduced yet (needs run/status data — S3+ adds it beside `Tag`).

**Forward gotchas for S3–S6 (authed mutations = server actions).**
- Authed mutations are **server actions**: call `requireIdentity()` **OUTSIDE** the action's `try` — it `redirect()`s by throwing, and a `try/catch` around it would swallow the redirect. A `"use server"` file may export **only** async functions (no consts/types) — put shared shapes elsewhere.
- The `/app/*` client goes in **`src/lib/knowledge/app.ts`** (new); extend `src/lib/knowledge/types.ts` for each `/app/*` serializer. Every call keeps `cache:"no-store"` and the bearer from the guard's `token`.
- `content/index.ts` grows per slice (add `dashboard`/`project`/`documents`/`graph` copy modules + extend the barrel). The dashboard's tenant name comes from the shell's cached `/auth/me` (no extra `GET /app/tenant`).
- S1's `NAV_LINKS`/`SECTION_IDS`/`SKIP_TO_CONTENT` (preview-only) are now unused by any page but remain exported; a later slice may prune them.

### Doc impact (S2)

- `frontend.md` — the BFF + session model (sealed `knowledge_session` AES-256-GCM httpOnly cookie), the `lib/knowledge/*` server-side client seam, the auth-guards + BFF pipeline, and the app-shell (dark login gate + light console shell + flat `AppButton` chrome; marketing pill reserved for P14).
- `architecture.md` — the sealed-cookie server-side BFF as knowledge's authenticated browser surface (no CORS change, no web-side DB); browser ↔ Next origin only, Next ↔ backend bearer.
- `experience.md` — the signup / login / logout UX + the authenticated nav (Dashboard live; Documents/Graph "Soon").
- `decisions.md` — D-P12-2 **as built** (Next.js server-side BFF proxy; sealed AES-256-GCM httpOnly cookie).
- `operations.md` — the two server-only env keys `KB_API_BASE_URL` (dev `http://127.0.0.1:8766`) + `SESSION_SECRET`, both blank/server-only via `.env.example`.

### Design gate — handoff to operator (Claude design co-work) · phase parked `pending`

**Protocol (how the design co-work works, per the operator).** The app's visual design is produced by the operator running a **Claude design co-work session** against the existing **Knowledge Base design system** project — *not* by the orchestrator iterating mockups or deciding the design. The engineering side's job here is only to **prompt** the design agent and then **wait for the brief** it hands back. The loop is: dedicated prompt → operator runs Claude design → **a design brief returns** → that brief drives the re-skin + the page slices.

- **Direction (agreed).** The app adopts the **Knowledge Base design system** — the calm editorial library (warm ivory & slate paper, one deep-teal accent, Fraunces / Source Sans / JetBrains Mono, hairline borders, single soft shadow) — carrying **hi2vi's dashboard structure/vibe** (paper topbar + TOC rail + stat tiles + data tables + status encoding). An **"editorial dashboard — a reading room's back office."** Both light + dark, token-driven.
- **This SUPERSEDES the S1 "adopt hi2vi_web green wholesale" record.** The S1 cross-slice note ("design system is now the ADOPTED hi2vi_web brand", bright green `#2ff28f` on near-black-green) is **no longer the target.** The app's real design = the **Knowledge Base design system**; hi2vi_web contributes **structure/vibe only**, not its green palette. S1/S2 currently wear the hi2vi green as a **placeholder** and will be **re-skinned** to the returned brief (tokens + component styling only — **no functional rework**).
- **Dedicated prompt written:** [`design-agent-prompt.md`](design-agent-prompt.md) — a self-contained brief for the Knowledge Base design-system agent: the five app surfaces (login / dashboard / project detail / documents / graph), the console component vocabulary (shell, stat tiles, data tables, status pills, fields, flat app-buttons, search, trend, show-once reveal), the identity constraints (both themes, teal-for-interactive-only, EN/KR), **three decisions to resolve** (serif vs. mono numerals · teal-only vs. semantic status colors · warm vs. dark login), the exact **returnable = a design brief** (both-theme CSS-custom-property tokens + self-hostable woff2 + canvas specimens + apply-map to the fixed component inventory), and real grounding data shapes.
- **Reference interpretation (optional, superseded by the brief):** a viewable round-1 mockup of this direction — KB editorial + hi2vi structure, both themes, the three open calls surfaced — exists at `https://claude.ai/code/artifact/c91b2540-f32d-48ad-8c7f-21f863a77f61`. It is a reference the operator may share with the design agent, **not** a spec; whatever the returned brief says wins.

**What happens when the brief lands (next steps, not started):** (1) **re-skin S1/S2** — swap `web/`'s tokens + component styling from hi2vi green to the brief's Knowledge Base system (globals.css `@theme`, the CVA primitives, the app-shell + auth pages, `AppButton`/status vocabulary), self-host the brief's fonts; (2) build **S3–S6** on that design. The re-skin likely wants its own slice (insert before S3 via `--order`, or fold into the S3 planning turn) — decided when the brief returns. **P12.REVIEW** consolidates the final design decision into `decisions.md` (final app design = Knowledge Base design system; the "adopt hi2vi green" half of the S1 D-P12-3 revision is itself superseded; the "deploy in P14" half still stands).

**Status: phase `pending` — waiting on the operator's design brief.** The `do-whole-phase` loop is halted here by design; it resumes (re-skin, then S3–S6) once the brief returns and the operator clears `pending` → `in_progress`.

### Cross-slice notes (S2R — Re-skin to the Knowledge Base design system)

**FINAL app design = the Knowledge Base "calm editorial library" design system.** The brief returned and is applied. **This supersedes the S1 "adopt hi2vi_web green wholesale" note** (bright `#2ff28f`/`#00b66a` on near-black-green): hi2vi contributes **structure/vibe only** (paper topbar + TOC rail + stat tiles + data tables + status encoding), **not** its palette. The whole `--color-green*` ramp now collapses to the single **deep-teal accent**. Both themes ship, token-driven.

**Now available to S3–S6 (build on these — do not re-derive Tailwind strings):**
- **`--kb-*` tokens for both schemes** (`src/app/kb-tokens.css`): paper/surface/surface-sunken/border/border-strong, the teal `--kb-accent`/`-strong`/`-soft`, ink/secondary/hint, the type scale + weights/tracking/leading, radii/spacing/motion/`--kb-shadow-hover`, the console structure sizes, and the additive **status/trend/delta** inks (active=teal · idle=amber-bronze · revoked=terracotta, encoded in FORM for greyscale; trend = teal; deltas up=teal/down=terracotta).
- **The portable `.kb-*` console classes** (`src/app/kb-console.css`, adopted from `console.css`): `.kb-app`/`.kb-topbar`/`.kb-app-layout`/`.kb-rail*`, `.kb-panel`, `.kb-appbtn--{primary,secondary,ghost,danger}`(+`--sm`)/`.kb-chip`, `.kb-tile-grid`/`.kb-tile`(+ Fraunces `.kb-tile__num`, `.kb-tile__delta--up/down`), `.kb-trend__*` (drawing spec in `web/design/canvas/components/console/console-trend.js`, for S3's TrendChart), `.kb-dtable` (+ `.num`/`.mono`/`.right`/`.kb-dtable__name`/`.kb-dtable__empty`), `.kb-status--{active,idle,revoked}` (+`--chip`), `.kb-field*`/`.kb-check`/`.kb-appsearch` (console search — S5), `.kb-reveal*` (show-once secret modal — **S4** credential mint), `.kb-toast`/`.kb-skel`/`.kb-empty`.
- Both stylesheets are `@import`ed from `globals.css`; the app's `@theme --color-*` is **repointed at `--kb-*`** (auto-recolor safety net for any utility not explicitly rewritten). NOTE: `.kb-*` CSS is **unlayered**, so it wins over Tailwind's layered utilities — override a `.kb-*` property with an inline `style`, not a Tailwind class.

**Scheme wiring (decision ③ — dark login, light app):** `<html>` base = `data-md-color-scheme="default"`; `(auth)` stage overrides to `="slate"` (the dark gate); the `(app)` shell carries `="default"` on its `.kb-app` root. Per-scheme values come free because `@theme` points at `--kb-*` and the tokens are defined per `data-md-color-scheme` block — resolved at the element, not the declaration.

**Icons: `lucide-react`** (already a dependency), **not** the specimens' iconify CDN; the console `svg` selectors are wired for it. This slice's surfaces need no icons; S3–S6 use lucide where icons are called for.

**Fonts:** self-hosted via `next/font/local` — **Fraunces** (`--font-fraunces` → display/headings + stat numerals) + **Source Sans 3** (`--font-source` → body) in; **Inter out** (`InterVariable.woff2` deleted, `--font-en` folded into the Source stack); JetBrains (mono) + **full Pretendard** kept as the Korean fallback ending every stack. Vendored: `web/public/fonts/{Fraunces,SourceSans3}.woff2`; brand `web/public/{logo,favicon}.svg`.

**KB design mirror:** `web/design/canvas/` now holds the KB handback (`APP_BRIEF.md` apply-map, `tokens/`, `components/console/`, `pages/`, `assets/`) — the source of truth the re-skin adopted, replacing S1's hi2vi placeholder canvas.

**Scope held:** tokens + component styling + fonts only, **no functional change** — auth BFF/guards/session/routes/content copy untouched; the 36 lib-level vitest tests pass unchanged. `ui/reveal.tsx` (scroll-reveal) had no colors → left as-is. `ui/button.tsx` (marketing pill, app-unused, P14) rides the repoint; its one hardcoded hi2vi-green **glow shadow** (which the repoint can't reach) was retuned to the KB `--kb-shadow-hover` token so zero hi2vi green remains in source or compiled CSS (see `result.md` deviation 1). `ui/section.tsx`'s `forest` tone was removed for the zero-`forest` verification (deviation 2).

### Doc impact (S2R)

- `frontend.md` — **the KB design system replaces the hi2vi_web green placeholder**: Tailwind v4 `@theme` repointed at the per-scheme `--kb-*` tokens (teal accent; both schemes), the `.kb-*` console layer (`kb-tokens.css` + `kb-console.css`) adopted from the design handback, self-hosted Fraunces + Source Sans 3 + JetBrains Mono (+ Pretendard Hangul fallback), and the per-route scheme (dark `slate` login gate → light `default` console). The app-shell/auth/`AppButton`/status/field components now compose on `.kb-*`; the marketing pill `Button` stays P14-reserved.
- `decisions.md` — **D-P12-3 FINAL: app design = the Knowledge Base design system** (the S1 "adopt hi2vi green" half of the D-P12-3 revision is itself superseded; hi2vi = structure/vibe only). The "deploy in P14" half still stands.
- `experience.md` — the dark "quiet threshold" login/signup gate → the light editorial workspace console (topbar + teal-active rail + Fraunces titles); status encoded in form (active/idle/revoked) for greyscale legibility.

### Cross-slice notes (S3 — Tenant dashboard: projects + create + tenant usage)

**New backend surface: `GET /app/dashboard`** (`server/dashboard_api.py`, mounted in `main.py` after `usage_api`). Session-guarded (`require_user`), **tenant-scoped, UNMETERED** (pure reads; never `record_event`). `days=Query(30, ge=1, le=365)`. One round-trip rollup so the browser needs no per-project fan-out. Response shape (pinned in the handler docstring): `{projects:[{id,name,created_at,documents,keys,last_used_at}], activity:[{type,at,project_name,credential_name}]}`. Assembly: `documents` = `documents_created` over the window (**not** a live per-project total — the content-plane UUID↔name bridge is **S5's** open question, deliberately not pulled in); `keys` = non-revoked credential count; `last_used_at` = max across the project's credentials; `activity` = real lifecycle events (`project_created`/`key_minted`/`key_revoked`), newest-first, capped 8. This is the isolated `/app` read-route addition DECOMP anticipated (extends the control-plane API; does **not** touch the D-P12-2 CORS/BFF boundary). **S4/S5/S6 can add their `/app/*` reads the same way.**

**Test harness (new ground for this repo — S4+ reuse it).** The repo had **no** accounts/usage pytest suite and **no async test harness** (no `pytest-asyncio`; the existing suite is the sync `fastapi.TestClient`). The accounts plane is **Postgres-only** (ORM `postgresql.UUID` columns — SQLite cannot back it). `tests/test_dashboard_api.py` is a **self-contained, env-driven Postgres test**: reads `KB_TEST_DATABASE_URL` (or `DATABASE_URL`) and **skips** when unset/unreachable (default `pytest` run stays green — no committed creds); creates tables with a throwaway **sync** engine (`Base.metadata.create_all`, so the app's async engine is never shared across event loops); seeds two tenants through the real HTTP surface (signup/create-project/mint/revoke) + a few direct `usage_events` inserts; drives the route with `TestClient`. Uuid-unique seed emails + tenant-scoped assertions → a shared DB never interferes. Proven green against a throwaway `kb_dashboard_test` DB on `127.0.0.1:55432` (vocky's Postgres — the only reachable one; knowledge has no host-mapped Postgres). **This is the accounts test pattern for the rest of P12.**

**Frontend built on the settled KB design (S3+ patterns):**
- **`lib/knowledge/app.ts` = the `/app/*` client seam** (was deliberately not ported in S2): `listProjects` / `createProject` / `getUsage` (bare, 30d) / `getDashboard` (bare). Thin typed wrappers over `client.ts`; `token` required first arg; `cache:"no-store"`; branch on `ApiError.status`. **S4 extends this file** with `getProject` / credential mint-list-revoke / `getProjectUsage`.
- **`types.ts` extended** with the `/app/*` shapes. NOTE the knowledge usage window is `{start,end}` (**not** vocky's `*_ingested_at`).
- **`components/usage/` (StatTiles + TrendChart)** are presentational, props-only, on the `.kb-*` classes — **S4's project page can reuse both** for per-project usage. TrendChart's geometry is a **faithful `console-trend.js` port** exposed as a pure `trendGeometry()` and unit-locked (`web/tests/trend-geometry.test.ts`). It uses a fixed gradient id (`kb-trend-fill`) — give a second chart on the same page a unique id to avoid a `<defs>` collision.
- **Create-project = server action** (`actions.ts` `"use server"` + `create-project-form.tsx` island): `requireIdentity()` **OUTSIDE the try**; a `"use server"` file exports only async fns (+ a type). **knowledge answers a blank/too-long project name as 422** (FastAPI body validation), not vocky's 400 — map **both** 400 and 422 to `invalidName`. Collapse-on-success is done inside the action transition (not a `useEffect`) because the repo's eslint `react-hooks/set-state-in-effect` rule forbids setState in an effect — the S4 credential forms should follow the same pattern.

**Design-fidelity deviation flagged for REVIEW:** the specimen's Projects panel head has a **"View all"** button, but **no all-projects route exists in P12** (rail has no Projects entry; project-detail is S4; the dashboard already renders the tenant's complete unpaginated list). A live "View all" would 404, violating the S2 "never a 404 link" console rule, so it is **omitted**. Everything else ships as designed. The review (or a later slice) should decide whether an all-projects surface + the button is wanted.

### Doc impact (S3)

- `api.md` / `architecture.md` — the new session-scoped, tenant-scoped, **unmetered** `GET /app/dashboard` aggregate route (per-project usage/credential rollup + lifecycle activity feed).
- `frontend.md` — the dashboard page + the `lib/knowledge/app.ts` `/app/*` client seam + the `components/usage/` StatTiles/TrendChart (line/area, `console-trend.js` port) + the projects DataTable + activity feed + the create-project server action.
- `experience.md` — the post-login dashboard UX (usage tiles / 30-day search trend / projects table / recent activity / create-project).

### Cross-slice notes (S4 — Project detail: info + credentials + project usage)

**Frontend-only — NO backend change.** Every endpoint already existed (`GET /app/projects/{id}`, `GET|POST|DELETE .../credentials[/{cid}]`, `GET .../usage`); S4 added zero server surface, so there is **no `api.md` Doc impact** and no new `/app` route (unlike S3's `/app/dashboard`). The dashboard's "Open" link (`/projects/<id>`) is now live.

- **Single fetch (cleaner than vocky's two).** knowledge's `GET /app/projects/{id}/usage` bundles **`project` + `credentials`** alongside `window`/`totals`/`daily_counts` (confirmed `server/usage_api.py` `_project_usage_response` — `payload["project"]` + `payload["credentials"]`), so the page makes ONE `getProjectUsage(token, id)` call and reads the header, the credentials table, and the usage from it. vocky needed a second `getProject` because its usage payload carried no `project`. `getProject` was still added to `lib/knowledge/app.ts` for completeness/reuse, but the page does not call it.
- **`lib/knowledge/app.ts` extended** (S3's seam): `getProject` / `createCredential` (returns the WHOLE `{credential, key}` envelope — never unwrap; `key` is the one-time plaintext) / `revokeCredential` (204) / `getProjectUsage`. `encodeURIComponent` on every path id; `token` first arg; `cache:"no-store"`; branch on `ApiError.status`. **`types.ts`** gained `KbCredential` / `KbMintedCredential` / `KbProjectUsage` (reusing S3's `KbUsageWindow`/`KbUsageTotals`/`KbDailyCount`).
- **Show-once `vk_` key — security core, AS BUILT.** The plaintext rides back ONLY in the mint action-state (`MintCredentialState.key`) — the one sanctioned server→client crossing — and is rendered exactly once by the `<ShowOnceKey>` modal. Grep-verified NOT logged (not even in the clipboard-copy catch), cached, persisted, cookied, or placed in a URL/storage; the list endpoint only ever returns `token_prefix`. Dismissal is keyed by the success **`ok` stamp** (`state.ok !== dismissedAt`), not a boolean, so the next minted key is never swallowed.
- **Show-once modal behavior built from scratch** (the KB handback ships no reveal JS, and `.kb-reveal-overlay` is `position:absolute`). Real viewport modal: a `createPortal(..., document.body)` with the overlay's `position` overridden to `fixed` via **inline style** (the S2R rule — inline beats the unlayered `.kb-*`); `role="dialog"` + `aria-modal`, a Tab focus-trap, **Escape-to-dismiss**, focus-in on mount + focus-return to the "New key" trigger on unmount, and a `navigator.clipboard.writeText` copy with a `select-all` manual fallback. Scrim-click does **not** dismiss (deliberate — an accidental outside click would destroy the key; Escape + the Dismiss button are the only exits, both explicit). Composed on the designed `.kb-reveal*` / `.kb-status__dot` classes.
- **3-state credential status (design-faithful, beyond vocky's 2).** Pure helper `lib/knowledge/credential-status.ts` `credentialStatus()`: `revoked_at→revoked` (wins even if also used) · else `last_used_at→active` · else `idle`, rendered via the existing `Badge` (`status`). Unit-locked in `web/tests/credential-status.test.ts` (3 terse cases). Kept a pure module (no `server-only`) so the server component and the Node-env vitest both import it.
- **Server actions** (`actions.ts`, `"use server"`): `mintCredentialAction` + `revokeCredentialAction`. `requireIdentity()` **outside** the `try` (redirects by throwing); `notFound()` likewise **outside** the fetch `try` (in `loadProject`); ids parsed as UUIDs (cheap rejection, not the boundary). knowledge answers a bad credential name as **422** (FastAPI body validation), so mint maps **400 AND 422** → `invalidName` (same as S3's create-project). Both actions `revalidatePath("/projects/[projectId]", "page")` (route pattern + `"page"`).
- **Design fidelity.** Composed entirely from the delivered `.kb-*` vocabulary — no project-detail specimen exists, so the page mirrors the dashboard card's patterns (`.kb-app-eyebrow`/`.kb-app-title`/`.kb-app-sub` header → `StatTiles` → trend `.kb-panel` → credentials `.kb-panel` + `.kb-dtable`) + vocky's structure. **Terracotta `.kb-appbtn--danger` revoke** (knowledge HAS it, unlike vocky's "secondary"), two-step inline confirm. Branded **`.kb-empty` not-found** (`not-found.tsx`), reached by `loadProject` mapping `ApiError` 404/400 → `notFound()`. **No new visual design invented** — flagged for the review as a faithful composition (a future design pass may refine the project-page layout). Reused S3's `components/usage/` StatTiles + TrendChart as-is (one usage block per page → no `kb-trend-fill` gradient-id collision).
- **Forward note (S5):** the credential list's `last_used_at` is the real recency stamp; the project header shows the single project `name` (no slug/chip — the dashboard mock's chip was illustrative). Live per-project document totals still await S5's content-plane UUID↔name bridge.

**Verification (all green):** `pnpm --dir web typecheck` ✓ · `lint` ✓ (0 warnings) · `test` ✓ (42 passed, incl. the 3 new + S2/S3 unchanged) · `build` ✓ (`/projects/[projectId]` compiles as `ƒ` Dynamic). Interactive E2E (mint → copy → dismiss → reload never re-reveals; revoke flips to Revoked; bad id → branded not-found) needs a running backend + seeded session — **not stood up** (same no-host-Postgres limitation as S3); left to the phase review.

### Doc impact (S4)

- `frontend.md` — the project-detail page (`app/(app)/projects/[projectId]/*`) + the `lib/knowledge/app.ts` `getProject`/`createCredential`/`revokeCredential`/`getProjectUsage` functions + the show-once `.kb-reveal` portal modal (viewport-fixed, focus-trapped, Escape-to-dismiss) + the derived 3-state credential status (`credential-status.ts` + `Badge`) + per-project usage via the single `getProjectUsage` fetch (bundles project + credentials) + the branded `.kb-empty` not-found.
- `experience.md` — the project-detail UX: project info · mint a show-once `vk_` key (copy-once, unrecoverable) / copy / revoke-with-inline-confirm (terracotta danger) · per-project usage tiles + search trend · the 3-state key status (active / idle / revoked, encoded in form for greyscale).
- (**No `api.md`** — S4 adds no backend surface.)

### Cross-slice notes (S5 — Per-tenant documents browse + search)

**Open question (a) RESOLVED (as built): new unmetered `/app` read routes, not the metered `/api/*`.** The app becomes the **per-tenant knowledge viewer**: browse (newest-first, optional project filter) + full-text search + read (rendered markdown), all on three new session-scoped, tenant-scoped, **UNMETERED** routes. Web-UI browsing/search therefore does **not** appear in the dashboard `searches` tile — that metric stays billable agent/API retriever usage, and web-UI features are free. This resolves the DECOMP "coexist-vs-replace the mkdocs viewer" question: per-tenant browsing lives in the app; the public mkdocs site stays tenant #1's public surface.

**New backend surface: `server/documents_api.py`** (mounted in `main.py` after `dashboard_api`), the third DECOMP-anticipated isolated `/app` read-route addition (no CORS/BFF change):
- **`GET /app/documents`** (`project?: UUID`, `tag?`, `limit=50 ≤200`, `offset=0`) → `{total, items}` newest-first · **`GET /app/documents/{doc_id:int}`** → the doc **with** `markdown` (404 missing/cross-tenant) · **`GET /app/search`** (`q` required, `project?: UUID`, `tag?`, `limit=10 ≤50`, `offset=0`) → `{query, mode, total, limit, offset, results}` (`SearchQueryError → 400`).
- **Reuses the S1 store/search AS-IS** (`db.list_documents`/`count_documents`/`get_document`, `search.search`) scoped with `tenant_id=str(ctx.tenant.id)`. **Unmetered = never sets `request.state.usage`** (the store/search layer is metering-free; verified by a before/after `usage_events` count test).
- **The project UUID→name bridge** (`_resolve_project_name`, the DECOMP-named work): a supplied `project` UUID is resolved via `AccountsService.get_project` scoped to the caller's tenant (**404 if missing/cross-tenant**, mirroring `app_api._load_scoped_project`), then the resolved **name** is what the store/search filter on (documents key off the project *name* string). **Reusable by S6** for a per-project graph scope.
- **`/app` projector `_app_doc`** drops `tags_text` + `tenant_id` (the `/api` `_public_doc` leaks `tenant_id`; this one must not) + `markdown` on the list. Search result items already exclude `markdown`/`tenant_id` (`search._finalize`), so they pass through unchanged. NOTE: the projected doc **keeps `related`** (forward `related:` rel_paths) — a minor superset of the plan's listed fields; the types include it (`KbDocumentListItem.related`), and **S6's graph edges may reuse it**.
- **GOTCHA (load-bearing): `get_conn` in `documents_api.py` is an `async def` generator dependency, on purpose.** These handlers are `async def` (they `await` the accounts service for the bridge), so the SQLite connection must be opened on the **event-loop thread** the handler uses it from. A sync `def` dependency (like `main.get_conn`) runs in FastAPI's threadpool and hands a connection created in another thread → `sqlite3.ProgrammingError` (check_same_thread). An async generator dependency fixes it cleanly. The existing `/api/*` routes avoid this only because they are sync `def` (whole handler + deps on one threadpool thread). **S6's `/app/graph` handler, if async + SQLite-backed, must use the same async `get_conn`.**

**Test harness (reused the S3 Postgres pattern; proven green).** `tests/test_documents_api.py` follows `tests/test_dashboard_api.py`: Postgres-gated (skips without `KB_TEST_DATABASE_URL`/`DATABASE_URL`), seeds the accounts plane via the real HTTP surface (signup/create-project) and the **content plane by writing documents straight into the throwaway SQLite** (`db.upsert_document`, `tenant_id` = the signed-up tenant's uuid). Five terse cases: list/detail shapes + newest-first + projector · the UUID→name bridge filter · tenant-isolation (cross-tenant doc id → 404, cross-tenant project UUID → 404, no list/search leak) · search shape · **unmetered** (before==after `usage_events`) · requires-auth (401). Verified green against a throwaway `kb_documents_test` DB on **vocky's Postgres** `127.0.0.1:55432` (user/pass `vocky` — knowledge still has no host-mapped Postgres; same limitation as S3/S4). **Full suite: 73 passed, 0 skipped with the DSN.**

**Frontend (built on the settled KB design, server-only like vocky's feedback surface):**
- **`lib/knowledge/app.ts` extended** (the `/app/*` seam): `getDocuments(token, query)` / `getDocument(token, id)` / `searchDocuments(token, {q,…})`. Query strings via `documentsQueryString` (a `URLSearchParams` port of vocky's `feedbackQueryString`: blank omitted, **no `encodeURIComponent` on top** or Korean double-encodes, bare path when empty). `token` first arg; `cache:"no-store"`; branch on `ApiError.status`. **`types.ts`** gained `KbDocumentListItem`/`KbDocument`/`KbDocumentsPage`/`KbSearchResult`/`KbSearchPage`/`KbDocumentsQuery`.
- **`lib/knowledge/documents-query.ts` (NEW, pure module)** — the `searchParams` round-trip + offset-pager math, extracted from the page (no `server-only`) so the Node vitest can import it (the S4 `credential-status.ts` precedent). `PARAM_KEYS` → `takeFirst` → `readActiveParams` (trim + **drop blanks** so an empty GET form means "no filter") → `toQuery`; plus **OFFSET** helpers `activeLimit`/`activeOffset`/`pagerOffsets` (clamp to `[0,total)`)/`documentsHref`. Unit-locked in `web/tests/documents-query.test.ts` (blank-dropping + pager clamp + href).
- **List page `app/(app)/documents/page.tsx`** — a `.kb-appsearch` GET form (magnifier + `type=search` `q` + project `<select>` whose value is the project **UUID** + Reset `<Link>`, not `type=reset`) → **branch on `q`** (present → `searchDocuments` ranked + snippet; else → `getDocuments` newest-first) → the `.kb-dtable` (Title link + optional snippet `.kb-dtable__sub` · Project name · Date mono · Tags `.kb-chip`) → **two empty states** (`hasFilters ? emptyNoMatches : emptyNoDocuments`) → an **offset pager** + an "N results" count. `listProjects` runs in parallel for the filter options. **Search-snippet rendering is XSS-safe**: the FTS `<mark>…</mark>` markers are split out and rebuilt with real `<mark>` elements; every other segment renders as an escaped string child (never `dangerouslySetInnerHTML`).
- **Read view `app/(app)/documents/[id]/page.tsx`** — `requireIdentity()` outside any try; a non-integer/non-positive id → `notFound()` (outside the try); a `loadDocument()` helper maps `ApiError` **404/400 → notFound()**. Back link + eyebrow(project)/Fraunces title/date header + a metadata strip (project/date/tags/source) + the rendered markdown body in a `.kb-panel`. Branded `.kb-empty` **not-found** at both `documents/not-found.tsx` (bad `?project=` filter) and `documents/[id]/not-found.tsx` (bad id); `lucide-react` icons.
- **Markdown reader (`markdown-body.tsx` + `prose.css`)** — `react-markdown@10` + `remark-gfm@4`, **no `rehype-raw` → XSS-safe by construction** (raw HTML is neutralized to inert escaped text — verified by a server-render smoke: `<script>` dropped, `<img … onerror>` escaped to `&lt;img…&gt;`, GFM tables render). A **server component** (zero client island; the build's page-data collection loads react-markdown in the RSC graph without a `"use client"`). Styled by a **minimal on-token `.kb-prose`** block (Fraunces headings / Source body / JetBrains code on `--kb-surface-sunken` / teal `--kb-accent` links / hairline blockquote+hr / GFM tables), all from `--kb-*` tokens.
- **Rail:** dropped `soon` from the Documents item (`content/app.ts`); `rail-nav.tsx` needed no change (active state already lit `/documents` + `/documents/[id]` via `startsWith`). **Documents is now LIVE in the rail.**

**Design fidelity (flagged for REVIEW):** no documents specimen existed in the KB handback (only dashboard + login), so both pages are a **faithful composition** of the delivered `.kb-*` vocabulary (`.kb-appsearch` — authored *for* this surface — `.kb-dtable`/`DataTable`, `.kb-empty`, `.kb-panel`, `.kb-chip`, `.kb-appbtn--*` pager, type helpers) following the dashboard patterns + vocky's structure — **no new visual design invented**. The one designed-vocabulary gap is the reader **`.kb-prose`** style, a minimal on-token extension (no reader/prose spec was designed) — flagged so a future design pass can formalize a reader spec.

**Forward notes (S6 / review):** the content store the graph slice will reuse is the same S1 SQLite (`db.py`) scoped by `tenant_id`; the **UUID→name bridge** (`_resolve_project_name`) and the **async `get_conn`** are ready to reuse for a per-project `/app/graph` route; the projected doc's **`related`** field (forward rel_path links) is exactly the edge data `scripts/graph_hook.py` inverts, so a per-tenant graph endpoint can build edges from `db.list_documents(...).related` + `tags` over a tenant's docs (no docs/ frontmatter walk needed). Metering stays opt-in per handler, so an `/app/graph` read is unmetered the same way (never set `request.state.usage`).

### Doc impact (S5)

- `api.md` — the three new **unmetered**, session-scoped, tenant-scoped `/app` read routes (`GET /app/documents`, `GET /app/documents/{id}`, `GET /app/search`) reusing the S1 store/search scoped by `tenant_id`, plus the project UUID→name bridge (404 on missing/cross-tenant) and the `/app` projector (drops `tags_text`+`tenant_id`, +`markdown` on the list).
- `architecture.md` — the app as the **per-tenant knowledge viewer** (resolves the "coexist-vs-replace the mkdocs viewer" question: per-tenant browsing lives in the app; the public mkdocs site stays tenant #1's public surface); web-UI reads are **unmetered** `/app` routes (no CORS/BFF change), and the content-plane project-name / control-plane project-UUID bridge.
- `frontend.md` — the documents browse/search/read surface (`app/(app)/documents/{page,not-found}.tsx` + `[id]/{page,not-found,markdown-body}.tsx` + `prose.css`) + the `lib/knowledge/app.ts` `getDocuments`/`getDocument`/`searchDocuments` seam + the pure `documents-query.ts` (searchParams round-trip + offset pager) + the markdown reader (`react-markdown`/`remark-gfm`, no `rehype-raw`, `.kb-prose`).
- `experience.md` — the documents UX (browse newest-first · project-filtered · full-text search with highlighted snippets · read rendered markdown) and the **Documents rail item going live**.

### Cross-slice notes (S6 — Knowledge graph in the web app, per-tenant)

**Open question (b) RESOLVED (as built): a new unmetered `/app/graph` route, server-side twin of the mkdocs hook.** The graph moves into the app as a **per-tenant** view. This is the LAST P12 middle slice; all six surfaces are now live.

**New backend surface: `server/graph_api.py`** (mounted in `main.py` after `documents_api`), the fourth DECOMP-anticipated isolated `/app` read-route addition (no CORS/BFF change):
- **`GET /app/graph`** — `require_user`-guarded, `tenant_id`-scoped, **UNMETERED** (never sets `request.state.usage` — the S3/S5 precedent, verified by a before/after `usage_events` count test). Fetches ALL the tenant's docs (`count_documents` then one `list_documents(limit=count)` — deliberately NOT the `/app/documents` 200 cap — bounded by `MAX_DOC_NODES = 2000`; a `truncated` bool flags an over-cap corpus).
- **`build_tenant_graph(docs)` is a faithful server-side TWIN of `scripts/graph_hook.py::build_graph`** — NOT an import. The hook must stay server-free (it runs inside `mkdocs build`, which installs only `mkdocs-material`; importing `server/*` would drag FastAPI/SQLite into the docs build). The twin runs the SAME inversion over the store's doc dicts (`db.list_documents` already decodes `related` + `tags`): doc/tag/missing nodes keyed on **`rel_path`**, related/tag edges (deduped, self-refs dropped, `broken` on dead targets), `degree`, and the load-bearing `projects` order `(-docs, name)`. **`scripts/graph_hook.py` is UNTOUCHED**; the public mkdocs graph stays tenant #1's surface.
- **The ONE substitution vs. the hook:** each doc node's **`url` = `/documents/{db_id}`** (the S5 read route), which is what makes a node click navigate in the app.
- **Reuses the S5 helpers by import** (`documents_api.get_conn` — the async connection dependency — and `_resolve_project_name` — the project UUID→name bridge). The route *accepts* an optional `project` UUID (404 on missing/cross-tenant) for symmetry, but the shipped UI sends none (per-tenant scope; narrowing would reclassify cross-project `related:` targets as ghosts, and none exist in the corpus today).

**Test harness (reused the S5/S3 Postgres pattern; proven green).** `tests/test_graph_api.py` **imports the `documents_client` fixture + `_signup`/`_project` from `test_documents_api.py`** (no duplicated engine boilerplate) and seeds docs with `related`/`tags` straight into the throwaway SQLite. Four terse cases: shape/inversion + the `url=/documents/{id}` rewrite + the `(-docs, name)` project order + degree + missing/broken ghost · tenant-isolation · unmetered · requires-auth. **Full suite with the DSN: 77 passed, 0 skipped** (vocky's Postgres `127.0.0.1:55432` — knowledge still has no host-mapped Postgres, same limit as S3–S5).

**Frontend — the app's first `"use client"` canvas + rAF component (a faithful port):**
- **`app/(app)/graph/graph-canvas.tsx`** ports `docs/javascripts/graph.js` (~1130 lines) verbatim in behavior — the deterministic force sim (FNV seeding, alpha cooling, tick integrator, collision relax), the draw grammar, and the full interaction model (drag/pan/hover-highlight, wheel zoom `{passive:false}`, Escape, legend project-lens + tag toggle, zoom buttons, node-tap→panel, sessionStorage persistence). Every colour/geometry token still read **live via `getComputedStyle` of `--kb-graph-*`**. Shell adaptations only: receives `data` as a **prop** (no fetch; `start(data)` in a `useEffect`); node nav rewrites `resolveUrl`→`node.url` directly (the absolute `/documents/{id}`); tag pills →`/documents?tag=<t>` (the S5 filter — no `tags/` route in the app); "Read the explainer →" → "Read the document →".
- **The critical React work = the `useEffect` teardown** (the original IIFE never tore down): `cancelAnimationFrame` the persistent rAF loop + the one-shot draw; `.disconnect()` the ResizeObserver + scheme MutationObserver; `removeEventListener` resize/pagehide/host-keydown; `clearTimeout` the persist debounce; a `disposed` flag guards the rAF loop + one-shot + `document.fonts.ready` against post-unmount. StrictMode's dev double-mount is handled by the full teardown. **A later canvas/rAF `"use client"` component should copy this teardown discipline.**
- **`--kb-graph-*` token layer (`graph-tokens.css`, co-located).** Maps `extra.css` §10a onto the base `--kb-*` palette for both schemes. **Near-exact by construction** — §10a was itself drawn from the KB palette, so most graph tokens resolve to a base token **byte-for-byte** (`project-1`=`--kb-accent`, `-2`=`--kb-status-idle`, `label`=`--kb-ink`, `focus`=`--kb-accent-strong`, `canvas`=`--kb-surface-sunken` light). Only graph-only inks stay literals (plum `project-3`, ghost/tag-edge neutrals, dark `edge-related`, accent-rgba halo, the bespoke dark plate `#16130f`, `--kb-graph-dim`). Overlay CSS (`graph.css`) ports §10b/c onto a sized `position:relative` container (dropped the mkdocs `100vw` full-bleed + `.md-*` `:has()` sidebar-hiding); the dangling `--md-default-fg-color--lightest` ref (extra.css:913) is fixed → `--kb-border`.
- **Client seam:** `getGraph(token)` in `lib/knowledge/app.ts` + `KbGraph`/`KbGraphNode`/`KbGraphEdge`/`KbGraphProject` in `types.ts` + `content/graph.ts` copy. **Rail:** dropped `soon` from the Graph item (`content/app.ts`); `rail-nav.tsx` unchanged. **Graph is now LIVE in the rail — the whole S2 rail is live.**

**Design fidelity (flagged for REVIEW):** no graph specimen existed in the KB handback (only dashboard + login), so the graph's look is ported faithfully from the shipped renderer + `--kb-graph-*` tokens (re-mapped onto the KB palette, near-exact to §10a). The interaction model + visual grammar are unchanged. The **`--kb-graph-*` token-layer composition is flagged** — a future design pass could formalize a graph spec (project inks, ghost dashes, halos, the quiet-label ladder), like S5's `.kb-prose` flag. The real interactive graph (drag/zoom/hover/click-through-to-a-doc) needs a running backend + browser and is **left to the phase review** (same no-host-Postgres limit as S3–S5).

**Deviations:** (1) the `/app/graph` response adds a `truncated` bool — a harmless superset of the four-key contract (the renderer ignores unknown keys); (2) tag-pill href + "explainer"→"document" wording (faithful shell adaptations, not a redesign). No other deviations; `scripts/graph_hook.py` untouched.

### Doc impact (S6)

- `api.md` — the new **unmetered**, session-scoped, tenant-scoped `GET /app/graph` (the content-store graph-data inversion — a server-side twin of `scripts/graph_hook.py` over `db.list_documents(tenant_id=…)`; nodes keyed on `rel_path`; each doc node's `url` = the S5 read route `/documents/{id}`; the `{version, projects, nodes, edges}` contract + a `truncated` flag; reuses the S5 `get_conn` + project UUID→name bridge).
- `architecture.md` — the knowledge graph moves into the app as a **per-tenant** view (content-store derived, not the build-time mkdocs asset — which stays tenant #1's public surface); web-UI graph reads are **unmetered** `/app` (no CORS/BFF change); the graph-data path is a server-side twin of the mkdocs hook (the hook stays server-free).
- `frontend.md` — the `GraphCanvas` `"use client"` component (the faithful port of the zero-dep `<canvas>` force-sim renderer, with the React teardown discipline) + the `--kb-graph-*` token layer (mapped onto the KB palette, both schemes) + the co-located overlay `graph.css` + `lib/knowledge/app.ts` `getGraph` + the `/graph` route; the Graph rail item now live.
- `experience.md` — the in-app graph UX (settle→mingle motion · drag/pan · wheel/pinch zoom · hover-neighbor-highlight + tooltip · legend project-lens + tag toggle · node-tap → info panel → read the document / tag filter) and the **Graph rail item going live** (the S2 rail is now fully live).

## Constraints

- **Surface discipline:** the app is a **session-token client of `/auth/*` + `/app/*` only** — never the `vk_`-keyed `/api/*` machine surface from its own auth flow. (S5 may deliberately choose the metered `/api/*` for documents/search with a session bearer as a fallback — that is a slice-turn decision, and the preferred path is new unmetered `/app` read routes.)
- **BFF boundary (D-P12-2):** browser talks only to the Next origin; sealed AES-256-GCM httpOnly cookie holds the backend token (never in browser JS); **no browser-JS token, no backend CORS change, no web-side DB.**
- **Credential plaintext (`vk_`) is shown exactly once** at mint — never re-rendered, never logged; only the sha256 hash + short prefix persist.
- **404-never-403** on foreign / cross-tenant ids (backend-enforced by `_load_scoped_project`); the app must not assume 403.
- **Usage is derived / read-only** (30-day default window); no billing, no plan-gating anywhere in the web UI (all web UI features free, graph included).
- **Design system:** Tailwind v4 CSS-first `@theme`, **no dark-mode toggle** (explicit surface tokens), copy-as-data `src/content/`, `cn()` with **extended tailwind-merge** registering the custom `text-*` scale (load-bearing), self-hosted `next/font`.
- **Placeholder palette** — token *names* kept swappable; scaffold `web/design/canvas/` but **defer the heavy Claude design gate AND production deploy to P14** (S1 = local-dev-runnable + `output: "standalone"` only).
- **Web-UI document browsing should stay unmetered** — prefer new session-scoped `/app` read routes over the metered `/api/*` (S5's call).
- **DECOMP writes no app code**, no slice `plan.md`, no `doc-new-version`, no commits.

## Open Questions

- **(a) S5 documents-read mechanism** — ~~new unmetered `/app` read routes vs. the metered `/api/*`~~ **RESOLVED (S5, as built): three new unmetered, session-scoped, tenant-scoped `/app` read routes** (`GET /app/documents`, `GET /app/documents/{id}`, `GET /app/search`) in `server/documents_api.py`, reusing the S1 store/search as-is scoped by `tenant_id`, with the control-plane `project` UUID → content-plane `project` *name* bridge (`_resolve_project_name`, 404 on missing/cross-tenant). Web-UI browsing/search stays out of the metered `searches` figure. See the S5 cross-slice notes.
- **(b) S6 graph-data source** — a new session-scoped `/app` graph endpoint reusing `scripts/graph_hook.py` inversion logic over the content plane vs. another path; and per-tenant vs. per-project scope. **Resolved at S6's planning turn** (also decides whether S6 bumps to `high`).
- **(c) `KB_API_BASE_URL` local-dev target** — host `:8766` vs container `:8000`. A **P14** concern (deploy), but S1/S2 need a working local default.
- **(d) Phase-size escape hatch** — whether the graph slice (S6) should spin into a follow-up phase if S1–S5 reveal P12 is oversized. Default: **keep in P12** per operator; only reconsider if the phase clearly overruns.

## Doc impact

Running list of durable-truth changes for **P12.REVIEW** to consolidate into new doc versions (the review executor confirms exact doc names/versions against `docs/index.json`; all names below exist today):

- `frontend.md` — **new version** (extends the existing mkdocs/graph frontend doc, not authored fresh): the ported design system (`@theme` tokens, `cn`, CVA primitives + `data-table`), the `web/` app structure, the BFF + auth/session model, and the component/page inventory (dashboard, project detail, documents browse, in-app graph).
- `architecture.md` — new version: separate-origin Next.js app + server-side BFF proxy as knowledge's **authenticated** browser surface (sealed httpOnly cookie, no CORS, no web-side DB); the new session-scoped `/app` read routes for documents/graph (if added by S5/S6); the app as the **per-tenant knowledge viewer** (resolves the "coexist-vs-replace mkdocs viewer" question).
- `api.md` — new version **if** S5/S6 add `/app` read routes (documents / search / graph): document them.
- `experience.md` — the dashboard / project-page / documents-browse / graph UX.
- `decisions.md` — D-P12-1/2/3 + the docs-browse-in-app / graph-in-app / deploy-in-P14 scope decisions.
- `operations.md` — web-app build/run stub (`pnpm dev`, `output: "standalone"`); full production deploy = **P14**.
- `product.md` (possibly) — the authenticated web app as the primary user surface; all web-UI features free.

_(Slices append their own Doc-impact lines here as they complete.)_

## Review outcome (P12.REVIEW)

**Verdict: `pass`** (executor `slice-executor-high`). All six middle slices validated
together and green; every constraint verified by code inspection; the eight durable-doc
versions consolidated. Full detail in `slices/P12.REVIEW/result.md`.

- **Validation re-run (not trusted from per-slice verdicts):** backend **77 passed, 0
  skipped** with the Postgres DSN (`postgresql://vocky:vocky@127.0.0.1:55432/postgres`) and
  **65 passed, 12 skipped** on the default no-DSN run (Postgres-gated `/app` suites skip
  cleanly); web **typecheck/lint** clean, **test 54 passed**, **build** OK (all routes
  compile); `workflow.py validate` passed.
- **Constraints held (verified):** surface discipline (`web/src/lib/knowledge/*` calls no
  `/api/*`; only its own `/api/auth` BFF); sealed httpOnly cookie (no browser-JS token);
  **no backend CORS middleware in `server/`**; the `/app` dashboard/documents/graph routes
  set **no `request.state.usage` and call no `record_event`** (unmetered); show-once `vk_`;
  404-never-403; read-only/no plan-gating; all web-UI features free.
- **Flagged deviations — all ACCEPTED, no fix slices:** (1) the S3 "View all" omission (no
  all-projects route; a live link would 404 — dashboard already lists the full set); (2)
  no design specimen for project-detail/documents/graph — faithful `.kb-*` compositions +
  the on-token `.kb-prose` (S5) and `--kb-graph-*` (S6) extensions (a future design pass may
  formalize reader/graph specs); (3) S2R's non-functional re-skin tweaks; (4) S6's
  `truncated` superset key + shell-wording adaptations.
- **Interactive/visual browser E2E:** deferred to the **P14 deploy / operator
  verification** (the sealed-cookie BFF + live uvicorn + seeded login + browser could not be
  stood up — the S3–S6 limit); recorded, **not a blocker**.
- **Docs consolidated (extend, not restate):** `frontend` v0005 · `architecture` v0010 ·
  `api` v0009 · `experience` v0005 · `decisions` v0013 · `operations` v0013 · `product`
  v0004 · `security` v0008 (source `P12.REVIEW`; `rebuild-docs` run; `docs/index.json`
  updated). `security` was promoted to a new version (plan left it to reviewer judgment) —
  the browser-facing sealed-cookie BFF + show-once credential + unmetered-reads model is new
  durable security truth beyond P10/P11.
- **Orchestrator next:** record with `review-phase P12 --verdict pass --reviewer
  slice-executor-high`, then `validate`, then commit. Phase stays in `active/` (archiving is
  a separate later step). Owed at P14: live-app visual acceptance + production deploy +
  public landing page + optional reader/graph design specimens.

- **S2** → `frontend.md` (BFF + sealed-cookie session + `lib/knowledge/*` client seam + auth-guards/pipeline + app-shell: dark login gate / light console / flat `AppButton`, marketing pill reserved for P14); `architecture.md` (sealed AES-256-GCM httpOnly-cookie server-side BFF, no CORS/no web-DB); `experience.md` (signup/login/logout + Dashboard-live / Documents-Graph-"Soon" nav); `decisions.md` (D-P12-2 as built); `operations.md` (server-only `KB_API_BASE_URL` `:8766` + `SESSION_SECRET`, via `.env.example`).
- **S2R** → `frontend.md` (**KB design system replaces the hi2vi green placeholder**: `@theme` repointed at per-scheme `--kb-*` tokens + the `.kb-*` console layer adopted from the design handback + self-hosted Fraunces/Source Sans 3/JetBrains/Pretendard + per-route dark-login/light-console scheme); `decisions.md` (**D-P12-3 final**: app design = Knowledge Base design system, hi2vi = structure/vibe only; the S1 "adopt hi2vi green" note superseded; "deploy in P14" still stands); `experience.md` (dark "quiet threshold" gate → light editorial workspace console; status encoded in form for greyscale). _(This supersedes the S1 Doc-impact "adopted hi2vi_web brand green" line above — the review consolidates `frontend.md`/`decisions.md` to the final KB system.)_
- **S3** → `api.md` / `architecture.md` (new session-scoped, tenant-scoped, **unmetered** `GET /app/dashboard` aggregate route — per-project usage/credential rollup + lifecycle activity feed; the first of the DECOMP-anticipated isolated `/app` read-route additions, no CORS/BFF change); `frontend.md` (the dashboard page + `lib/knowledge/app.ts` `/app/*` client seam + `components/usage/` StatTiles/TrendChart — line/area `console-trend.js` port — + projects DataTable + activity feed + create-project server action); `experience.md` (the post-login dashboard UX: usage tiles / 30-day search trend / projects table / recent activity / create-project). _(Deviation for the review: the specimen's "View all" projects button is omitted — no all-projects route exists in P12.)_
- **S4** → `frontend.md` (the project-detail page `app/(app)/projects/[projectId]/*` + `lib/knowledge/app.ts` `getProject`/`createCredential`/`revokeCredential`/`getProjectUsage` + the show-once `.kb-reveal` portal modal — viewport-fixed, focus-trapped, Escape-to-dismiss — + the derived 3-state credential status `credential-status.ts` × `Badge` + per-project usage via the single `getProjectUsage` fetch that bundles project + credentials + the branded `.kb-empty` not-found); `experience.md` (the project-detail UX: project info · mint a show-once `vk_` key / copy / revoke-with-inline-confirm on terracotta danger · per-project usage tiles + search trend · the 3-state key status active/idle/revoked encoded in form). _(**No `api.md`** — S4 is frontend-only, adds no backend surface. Design note for the review: the project-detail page is a **faithful composition** of the delivered `.kb-*` vocabulary — no project-detail specimen was in the handback — following the dashboard card + vocky structure; a future design pass may refine it.)_
- **S5** → `api.md` / `architecture.md` (three new **unmetered**, session-scoped, tenant-scoped `/app` read routes — `GET /app/documents`, `GET /app/documents/{id}`, `GET /app/search` — in `server/documents_api.py`, reusing the S1 store/search scoped by `tenant_id`, with the control-plane project-UUID → content-plane project-name bridge and the `/app` projector dropping `tags_text`+`tenant_id`+list-`markdown`; resolves the DECOMP "per-tenant knowledge viewer" question — app browsing is unmetered, the public mkdocs site stays tenant #1's); `frontend.md` (the documents browse/search/read surface + `lib/knowledge/app.ts` `getDocuments`/`getDocument`/`searchDocuments` + the pure `documents-query.ts` searchParams-round-trip + offset pager + the `react-markdown`/`remark-gfm` reader with the on-token `.kb-prose` block, XSS-safe with no `rehype-raw`); `experience.md` (browse newest-first · project-filtered · full-text search with highlighted snippets · read rendered markdown; Documents rail item now live). _(Design note for the review: both pages are a **faithful composition** of the delivered `.kb-*` vocabulary — no documents specimen was in the handback — and `.kb-prose` is a minimal on-token reader extension flagged for a future design pass to formalize.)_
- **S6** → `api.md` / `architecture.md` (new **unmetered**, session-scoped, tenant-scoped `GET /app/graph` in `server/graph_api.py` — a server-side twin of `scripts/graph_hook.py`'s inversion over `db.list_documents(tenant_id=…)`, emitting the same `{version, projects, nodes, edges}` contract (+`truncated`) with nodes keyed on `rel_path` and each doc node's `url`=the S5 read route `/documents/{id}`; reuses the S5 `get_conn` + project UUID→name bridge; the mkdocs hook stays server-free/tenant-#1's public surface — the graph is now a **per-tenant** app view); `frontend.md` (the `GraphCanvas` `"use client"` canvas force-sim component — a faithful port of `docs/javascripts/graph.js` with the critical React useEffect teardown — + the `--kb-graph-*` token layer mapped onto the KB palette for both schemes + the co-located overlay `graph.css` + `lib/knowledge/app.ts` `getGraph` + the `/graph` route); `experience.md` (the in-app graph UX: settle→mingle motion · drag/pan · wheel/pinch zoom · hover-highlight+tooltip · legend project-lens + tag toggle · node-tap → info panel → read the document / tag filter; the Graph rail item now live — the whole rail is live). _(Design note for the review: no graph specimen was in the handback — the graph is a faithful port of the shipped renderer + `--kb-graph-*` tokens re-mapped onto the KB palette (near-exact to `extra.css` §10a); the token-layer composition is flagged for a future design pass, like `.kb-prose`. Deviations: a `truncated` superset key; tag-pill href + "explainer"→"document" wording. Interactive graph E2E left to the review — same no-host-Postgres limit as S3–S5.)_
