# Phase P18: Accounts v2: user/org/project with org-level keys

_Intent: see [intent.md](intent.md)._

## Objective

Restructure the accounts plane to an explicit user → org → project model: signup auto-provisions a "default" org and "default" project; API keys become mintable at org level (making today's de-facto tenant-wide enforcement honest); project resolution is get-or-create by name; knowledge save keeps the repo-basename default with a --project override and "default" fallback. Org creation + member invites stay out of scope (deferred).

## Context

Accounts-plane map, verified against the code on 2026-07-22 (line refs are anchors, re-verify before editing — the tree moves):

- **Two planes.** *Control plane* = Postgres (tenant mode iff `DATABASE_URL`): `users`, `tenants` (name only; ownership via `tenant_members` role=owner), `projects` (tenant_id, name — **no UNIQUE(tenant_id,name)** today), `project_credentials` (project_id FK **NOT NULL**, name, token_prefix, token_hash sha256 UNIQUE, last_used_at, revoked_at — no tenant_id, no expiry/scopes), `auth_tokens`, `usage_events`. ORM `server/persistence/models.py` (verified: `ProjectModel` has only `Index("ix_projects_tenant_id")`; `ProjectCredentialModel.project_id` is non-nullable FK). Migrations `alembic/versions/0001_accounts_tenancy.py`, `0002_usage_events.py`; applied **manually** in prod (`docker compose exec api alembic upgrade head`, then `python -m server.seed`). *Content plane* = disposable SQLite: `documents.tenant_id TEXT DEFAULT ''` sentinel, `UNIQUE(tenant_id, rel_path)`; rebuilt from files on boot; self-migrates in `server/db.py:init_db`.
- **Signup** `server/auth_api.py:215-238`: argon2id, tenant named `f"{email.localpart}'s workspace"` via `service.create_tenant_with_owner(user.id, name)` (`server/accounts/service.py:99-126` — tenant + owner `tenant_members` row atomically, no project), 30-day session token, **no project created**. Per-IP throttle on signup/login.
- **Minting** `server/app_api.py:148-170` `POST /app/projects/{id}/credentials`: `vk_` + `generate_opaque_token()`; sha256 hash + `key[:12]` prefix stored; plaintext returned once. `list_credentials` GET at `:173`. Web UI `web/src/app/(app)/projects/[projectId]/mint-credential-form.tsx` + `revoke-credential-button.tsx` + `actions.ts`.
- **Resolver** `server/api_auth.py:130-175` `_resolve_tenant_bearer`: (1) master `KB_API_TOKEN` → tenant #1 (`get_tenant_one_id`, cached on first success); (2) `vk_` credential → `service.get_active_credential_by_token_hash` then `service.get_project(cred.project_id)` → `ApiAuthContext(tenant_id=project.tenant_id, project_id, credential_id)` — **so authorization is already tenant-wide; the project binding only carries project_id for attribution**; a credential pointing at a vanished project is unresolvable; (3) session token → user's first tenant. `ApiAuthContext(tenant_id, project_id, credential_id, is_public)`.
- **Write path** `server/main.py:389-592` `POST /api/documents` (`resolve_api_write` guard): any resolvable credential authorizes (tenant-level); `body.project` free-form, validated for shape but **never checked against the credential**; routes to `_tenant_root(ctx)` / `_tenant_db_id(ctx)`; public tenants get the canonical `docs/` tree + landing + Recent, others a namespaced `tenants/<uuid>/`. **No Postgres `projects` row is created here** — saving an unregistered name leaves no registry row. Metering `server/usage/metering.py:50-98` (`record_usage`): resolves project by `get_project_by_name(tenant_id, project_name)` **first**, falls back to `hint.project_id`, else tenant-level NULL — it *reads* the row, never creates it.
- **Seed** `server/seed.py`: idempotent; operator user + tenant #1 (`"<localpart>'s workspace"`) + a `projects` row per live `docs/` project (derived from the tree by reindex's filename rule, not hardcoded). **No `vk_` seeded** (master bearer suffices).
- **CLI** `cli/src/knowledge_cli/knowledge.py`: `--project` **already exists** on `save` (:632-635) and is threaded through (`args.project or default_project()`, :378); `default_project()` (:93-109) = git-repo-root basename → `auth.stored_project()` → `auth.DEFAULT_PROJECT`. **`auth.DEFAULT_PROJECT = "knowledge"`** (`cli/src/knowledge_cli/auth.py:64`) — also the default for `init --project` (:585). So the CLI's outside-a-repo fallback is `"knowledge"` today, **not** `"default"`. Project sent as a JSON body field (`client.py:282`).
- **Skills.** Explain skill canonical `plugin/skills/explain/SKILL.md` (project = repo dir name verbatim; env `KB_API_BASE_URL`/`KB_API_TOKEN` precedence). Setup skill `plugin/skills/setup/SKILL.md` (~:48-51: "one vk_, all repos; the key's own bound project is only how usage is attributed").
- **Web app** (P12 design system): topbar/nav "Workspace" copy in `web/src/content/app.ts:33,35` + `dashboard.ts:33,35` + `auth.ts:57,73` ("A workspace is created for you automatically") + `documents.ts:9,11,77`. Per-project keys table + mint modal already exist (`web/src/app/(app)/projects/[projectId]/`).
- **Parity gates (P17, CI `plugin-ci.yml`):** `scripts/plugin_parity.py` — `plugin/templates/manifest.json` `shipped_dirs = ["server", "tests", "docs/assets", "docs/stylesheets", "docs/javascripts"]` must match `plugin/templates/kb/<dir>/` **set-for-set and content-identical**. **`alembic/` is NOT a shipped_dir** (template has no alembic dir) — migrations stay repo-only, but **every `server/**` and `tests/**` change must be mirrored into `plugin/templates/kb/` in the same slice.** `scripts/skills_parity.py` — the explain skill's two copies `plugin/skills/explain/SKILL.md` ↔ `.agents/skills/explain/SKILL.md` must stay body-identical.
- **Invariants:** single uvicorn worker (write lock + in-process rate limiter depend on it); frozen public contract `/api/*` + `/auth/*` + `/app/*` — **additive only**; tests pytest (accounts coverage Postgres-gated via `KB_TEST_DATABASE_URL`, else skips); E2E `scripts/onboarding_smoke.py`; prod deploy `deploy/deploy.sh` (api runs from bind mount; schema changes are manual alembic + seed).

## Decomposition

Five middle slices (S1..S5), orders 1-5; DECOMP=0, REVIEW=9999 stays last. Backend first (S1 schema → S2 behavior), then the two client surfaces (S3 web, S4 CLI/skills — parallel-safe, both depend only on S2), then the operator-gated prod cutover + E2E (S5). Every slice that edits `server/**` or `tests/**` mirrors into `plugin/templates/kb/` in the same slice (parity). No slice is `low` — every one carries schema, hot-path, cross-copy-parity, or prod judgment.

### P18.S1 — Schema 0003 + models + signup/seed default-org/default-project provisioning (risk: high, order 1)

**Scope.** The control-plane foundation everything else builds on: (a) alembic `0003` migration — `UNIQUE(tenant_id, name)` on `projects` (get-or-create needs it; must **defensively de-dupe pre-existing duplicate names** before adding the constraint), add `project_credentials.tenant_id` (FK `tenants`, **backfilled from each row's `project.tenant_id`**), make `project_credentials.project_id` **nullable** (org-level rows will carry `project_id NULL`); (b) mirror those column/constraint changes in `server/persistence/models.py` (`ProjectModel` UniqueConstraint, `ProjectCredentialModel` tenant_id + nullable project_id); (c) signup (`server/auth_api.py`) provisions org `"default"` + project `"default"` in one transaction — introduce/extend an accounts-service primitive (e.g. `create_tenant_with_owner` gains a default-project insert, or a new `provision_default_workspace`) so tenant + owner + `"default"` project commit atomically, replacing the `"<localpart>'s workspace"` naming with `"default"`; (d) reconcile `server/seed.py` (idempotent; existing tenant #1 keeps its name per design stance #1 — labels only; decide whether the seed also ensures a `"default"` project for tenant #1 or leaves it to lazy get-or-create); (e) mirror all `server/**` edits into `plugin/templates/kb/server/`; Postgres-gated accounts tests for provisioning + the new columns (mirror any `tests/**` additions into the template).

**Rationale.** S2's resolver reads `credential.tenant_id` directly and S2's write path get-or-creates against `UNIQUE(tenant_id,name)` — both are impossible until the columns/constraint exist. Signup provisioning is the user-visible core of the intent and belongs with the schema that makes `"default"` project a real registry row.

**Risk = high.** A live alembic migration on prod data: a new UNIQUE constraint that fails outright if duplicate `(tenant_id,name)` rows exist, plus a backfill of `project_credentials.tenant_id`, plus a nullability change on a hot FK — this needs a defensive, reversible migration and full judgment. Signup is a transactional primitive on the auth path. Not mechanical.

### P18.S2 — Org-level keys: resolver + mint endpoint + write-path get-or-create + metering (risk: high, order 2, depends P18.S1)

**Scope.** Make org-level keys honest and projects get-or-create: (a) resolver `server/api_auth.py:153-164` reads `cred.tenant_id` **directly** (org-level rows have `project_id NULL`; project-bound rows still populate `project_id` for attribution) — must keep every existing project-bound key working unchanged and preserve the "credential → vanished project → unresolvable" guard only where a `project_id` is actually set; (b) a new **additive** org-level mint endpoint (e.g. `POST /app/credentials` or `POST /app/orgs/{id}/credentials`) that mints a `vk_` with `tenant_id` set + `project_id NULL`, plus its list route — additive-only to the frozen `/app/*` contract; (c) write-path get-or-create — when `POST /api/documents` accepts `body.project`, ensure a Postgres `projects` row exists for `(ctx.tenant_id, project)` (get-or-create, race-safe against the new UNIQUE), so unregistered names stop leaving the registry blank (decide the seam: `server/main.py` write path vs. inside `record_usage`/service — prefer an explicit get-or-create in the write path so the row exists even for a save that isn't metered); (d) align `server/usage/metering.py` to the get-or-create row; (e) mirror all `server/**` edits into `plugin/templates/kb/server/`; Postgres-gated tests (org-key mint+resolve, project-bound key still works, get-or-create idempotency) mirrored into the template.

**Rationale.** These are the three behavioral halves of the intent's points 2 + 3, all sitting on S1's schema, and all touching the same resolver/write/metering call chain — splitting them would thrash the same files across slices.

**Risk = high.** The resolver is the hot auth path on every `/api/*` request; a regression there breaks the live hi2vi agent and every vk_ user. New endpoint + write-path get-or-create with UNIQUE-race handling. Full judgment, prod-regression-sensitive.

### P18.S3 — Web app: org-level keys surface + workspace→org copy (risk: medium, order 3, depends P18.S2)

**Scope.** (a) An org-level keys surface reusing the **existing** P12 keys-table + mint-modal + revoke components at org scope (mint/list/revoke org `vk_` keys via S2's endpoint) — decide where it lives (dashboard section vs. a dedicated org-settings route) using existing design-system components; (b) the BFF read/write routes for org-level credentials mirroring the project ones (`web/src/app/(app)/projects/[projectId]/actions.ts` pattern); (c) rename user-facing "Workspace" → "Org" copy in `web/src/content/app.ts`, `dashboard.ts`, `auth.ts` (signup: "A workspace is created for you automatically" → org wording), `documents.ts` (label-only).

**Rationale.** Web is a client of S2's endpoint, so it follows S2. Copy + org-keys surface are one cohesive web concern; keeping them in one slice avoids two Next.js round-trips.

**Risk = medium.** Web-only, no schema/auth-hot-path exposure, and it reuses existing components — but it adds a new surface + BFF wiring and needs judgment on placement, so not mechanical. **Design note:** per design stance #6, reusing the existing keys-table/mint-modal at org scope is the *same* visual language at a different scope, **not** a new design surface → **no Claude Design round**. If S3's planning concludes a genuinely new-design surface is required (e.g. a net-new org-settings information architecture), it must flag that in this phase.md and stop rather than invent visual design.

### P18.S4 — CLI --project/default fallback + explain/setup skill text + parity (risk: medium, order 4, depends P18.S2)

**Scope.** (a) CLI outside-a-repo fallback → `"default"`: `default_project()` (`knowledge.py:93-109`) currently falls back to `auth.DEFAULT_PROJECT = "knowledge"`. **Decide** whether to introduce a distinct save-fallback constant (`"default"`) leaving `init --project`'s `"knowledge"` default untouched, or to retarget `DEFAULT_PROJECT` — `--project` already exists, so this is the one real behavior change (favor a separate save-fallback so P13's `init` default is not silently changed; record the decision). (b) Update explain skill + setup skill text for the org-key model (get-or-create projects, one org `vk_` serving every repo, repo-basename default with `--project` + `"default"` fallback). (c) **skills_parity**: keep `plugin/skills/explain/SKILL.md` and `.agents/skills/explain/SKILL.md` body-identical; update the setup skill copy(ies) consistently.

**Rationale.** CLI + skills describe the same user-facing model (get-or-create, org keys, default fallback), so they change together. Independent of the web surface — both S3 and S4 depend only on S2, so they can run in either order.

**Risk = medium.** Mostly text, but the `DEFAULT_PROJECT` decision is a real judgment call (it can silently change `init`'s behavior) and the dual explain-copy parity must stay byte-exact — not fully mechanical, so not `low`.

### P18.S5 — Prod migration + deploy + extended onboarding E2E (operator-gated) (risk: high, order 5, depends P18.S2, P18.S4)

**Scope.** (a) Extend `scripts/onboarding_smoke.py`: new signup → auto default org + default project → mint an **org** key → save via repo-basename / `--project <name>` / outside-repo `"default"` → assert an **old project-bound key still works** → verify get-or-create registered the project. (b) Operator-gated prod cutover: prod `alembic upgrade head` (0003) + `python -m server.seed` + `deploy/deploy.sh` are operator-run — set the slice `pending`, hand the operator the exact commands, STOP, then verify the live E2E on resume. (c) Post-deploy verification (freshness self-assert per the P17 deploy pattern).

**Rationale.** The prod migration/seed/deploy must run after all code slices merge; the extended E2E is the phase's end-to-end proof and is the natural home for the operator gates. Isolating prod risk in the last slice keeps the code slices commit-clean.

**Risk = high.** Live migration on prod data + operator-coordinated deploy + full E2E. Full judgment + operator co-work (`pending`).

## Findings & Notes

Seeded by P18.DECOMP (2026-07-22). Cross-slice decisions later slices must honor:

- **No `tenants` → `orgs` rename** in either plane (design stance #1, confirmed against the code). The tenant id threads through SQLite `documents.tenant_id`, `usage_events`, the resolver, seed, MCP, and the web BFF; a rename is a large mechanical diff with prod-regression risk and **zero behavior change**. The tenant row **is** the org. Product-facing naming becomes "org" in web copy, docs, and skill text only. → **decisions-doc impact.**
- **Org-level credentials are additive.** Add `project_credentials.tenant_id` (backfilled from the bound project's tenant), make `project_id` nullable; org-level rows = `project_id NULL`; existing project-bound keys keep working; the resolver reads `tenant_id` directly. No column is dropped, no existing row rewritten in meaning.
- **Get-or-create requires `UNIQUE(tenant_id, name)` on `projects`** — the 0003 migration must de-dupe pre-existing duplicate names defensively before adding the constraint (prod may already hold dupes because there is no unique today).
- **Signup provisions `"default"` org + `"default"` project atomically**; the seed stays idempotent and existing tenants keep their names (labels only), gaining `"default"` lazily via get-or-create. Existing tenant #1 is **not** renamed.
- **`--project` already ships on the CLI** (P13); the only CLI behavior change is the outside-a-repo fallback (`"knowledge"` → `"default"`) — and `DEFAULT_PROJECT` is shared with `init --project`, so S4 must not change `init`'s default as a side effect.
- **Verified de-facto tenant-wide enforcement:** the resolver already returns `ctx.tenant_id` for a `vk_` key; the write path authorizes by tenant, never checking `body.project` against the credential — so "org-level keys" makes existing behavior honest, it does not loosen enforcement.
- **Parity is per-slice, not deferred to review:** any `server/**` or `tests/**` edit must be mirrored into `plugin/templates/kb/` **in the slice that makes it** (CI `plugin_parity` fails otherwise). `alembic/` is repo-only (not a shipped_dir). Explain-skill edits must keep both copies body-identical (`skills_parity`).

### S1 landed (2026-07-22) — notes for S2–S5

- **`provision_signup(user_id)` is the shared primitive** (`server/accounts/service.py`): tenant `"default"` + `owner` membership + project `"default"` atomically; returns `(TenantRecord, TenantMemberRecord, ProjectRecord)`. Constants `DEFAULT_ORG_NAME` / `DEFAULT_PROJECT_NAME` (both `"default"`) live there too. Signup and fresh-DB seed both call it; `create_tenant_with_owner` is kept for bare-tenant callers. **S2's write-path get-or-create should reuse the repo/service seam, not re-provision.**
- **Schema after 0003:** `projects` has `UNIQUE(tenant_id, name)` (`uq_projects_tenant_id`) — S2's write-path get-or-create must be race-safe against it (catch the unique violation and re-read). `project_credentials.tenant_id` is **NOT NULL** (FK tenants CASCADE, indexed); `project_credentials.project_id` is **nullable**. **S1 writes NO org-level (NULL project_id) rows** — every credential is still project-bound. S2 introduces the first NULL-project_id rows via the org-mint endpoint.
- **`ProjectCredentialRecord.project_id` / `CreateProjectCredential.project_id` are already `UUID | None`.** But `app_api.serialize_credential` still does `str(record.project_id)` — **S2 MUST make it NULL-safe** before minting org-level keys (else org rows serialize `project_id` as the string `"None"`). Likewise the resolver `server/api_auth.py:153-164` still does `service.get_project(cred.project_id)` unconditionally — **S2 must read `cred.tenant_id` directly and only chase `project_id` when it is set**.
- **Signup response gained additive `project`** (`{id, name, tenant_id, created_at}`) alongside `tenant`. Frozen contract respected (additive only). **S3's web signup/BFF can rely on `project` now.** `serialize_project` exists in BOTH `app_api` (canonical, imported by `usage_api`) and `auth_api` (local copy, byte-identical output — avoids the `app_api → auth_api` import cycle); keep them in sync if either changes.
- **Seed stance confirmed in code:** fresh DB → tenant #1 named `"default"` (+ default project) via the primitive; existing DB → tenant kept verbatim, **prod tenant #1 is NOT renamed** (S5's prod seed run keeps hi2vi's existing tenant name; it only adds a `"default"` project lazily / if it is a live `docs/` project).
- **Migration is de-dupe-safe and reversible.** `0003` de-dupes duplicate `(tenant_id, name)` projects (oldest-wins, re-points credentials + usage_events, deletes dupes) BEFORE adding the UNIQUE — validated against a disposable Postgres incl. a seeded-dupe scenario, an upgrade/downgrade round-trip, and the full gated suite. **S5's prod `alembic upgrade head` is the real de-dupe run** — worth a `SELECT tenant_id, name, count(*) … GROUP BY … HAVING count(*)>1` pre-check on prod so the operator knows what will be merged.
- **PRE-EXISTING gated failure (NOT P18):** `tests/test_documents_api.py::test_documents_list_detail_and_project_bridge` fails on an extra `format` key in the document-list projection — **confirmed failing on a clean tree (stash test)**, so it predates P18 and only surfaces when Postgres-gated tests actually run (default CI has no Postgres). Out of S1's accounts scope; left unfixed. **Review/operator: decide whether `format` belongs in list items (update `_LIST_KEYS`) or is a projection bug — a candidate small fix slice or deferred job.** Note: running the gated suite requires a Postgres (I used a throwaway `postgres:17` docker container on `:55432`).

### S2 landed (2026-07-22) — notes for S3–S5

- **Final org-credential endpoint paths (S3's BFF mirrors these):** `POST /app/credentials` (201, `{credential, key}`, show-once) mints an org key (`project_id NULL`); `GET /app/credentials` returns `{credentials: [...]}` (org-level only, includes revoked, oldest-first); `DELETE /app/credentials/{credential_id}` (204, 404 if the id isn't the caller's own org-level credential). All gated by `Depends(require_user)`, scoped to `ctx.tenant.id`. These sit **alongside** the per-project routes (`/app/projects/{id}/credentials`) — additive, both still work. **S3 wires org-scoped BFF routes mirroring `web/src/app/(app)/projects/[projectId]/actions.ts`, reusing the P12 keys-table + mint-modal at org scope (no new design round per stance #6).**
- **`serialize_credential` is now NULL-safe** (`project_id: null` for org keys, UUID string for project-bound). It is the **shared** serializer imported by `usage_api.py` too — the change is backward-compatible, but S3's TS types for a credential must treat `project_id` as `string | null`.
- **Resolver reads `cred.tenant_id` directly.** Org key (`project_id NULL`) → tenant-scoped auth, no bound project; project-bound key keeps the vanished-project existence guard (only chased when `project_id` is set). Master/session paths untouched. **No trust-boundary change** — enforcement was already tenant-wide (a `vk_` already resolved to `ctx.tenant_id` and the write path never checked `body.project` against the credential); org-level keys make the de-facto behavior honest. → security doc = wording update, not a boundary shift.
- **Write path now get-or-creates the registry project** via an **async** dependency `ensure_registry_project` in front of the sync `create_document` (the handler holds `WRITE_LOCK` and can't await Postgres; FastAPI shares the parsed body + caches `resolve_api_write`, so the dep runs once, before the handler). An unregistered `body.project` now always leaves a `projects` row. The row may be created even if the handler then 409s/422s on other fields — idempotent and harmless. `get_or_create_project` is race-safe against `UNIQUE(tenant_id, name)` (catch `IntegrityError` → rollback → re-read). **S5's extended E2E can now assert `/app/projects` shows a saved-to name that was never explicitly created.**
- **Latent dupe-500 killed:** `POST /app/projects` now routes through `get_or_create_project`, so a duplicate name returns the existing row (201) instead of the `IntegrityError` → `AccountsPersistenceError` → 500 that S1's UNIQUE had introduced.
- **`metering.py` untouched** — its name-first `get_project_by_name` lookup now always finds the row the write get-or-created; the `UsageHint.project_id` (`registry_project_id or ctx.project_id`) is only the fallback.
- **New Postgres-gated test:** `tests/test_org_credentials.py` (mirrored into the template + `manifest.json`). Needs both a Postgres DSN (`KB_TEST_DATABASE_URL`) **and** an on-disk `KB_ROOT` (it drives real `POST /api/documents`). Non-public tenant → writes land in `tenants/<uuid>/` and git commit auto-skips, so no git repo is needed.

### S3 landed (2026-07-22) — notes for S4–S5 + REVIEW

- **No new design surface / no Claude Design round** — confirmed stance #6 in code: the Org API keys panel reuses P12's keys-table + mint-modal + revoke components at org scope. Placement: a **full-width `kb-panel` on `/dashboard`** (`web/src/app/(app)/dashboard/page.tsx`) below the projects/activity grid — **no new route, no rail change**. Project pages keep their per-project keys unchanged.
- **`KbCredential.project_id` is now `string | null`** (`web/src/lib/knowledge/types.ts`) — the honest shape of S2's NULL-safe `serialize_credential`. This forced one type-narrow on the **project detail page** (`projects/[projectId]/page.tsx`): the revoke cell now guards `credential.project_id !== null` (behavior identical — a project-detail credential is always project-bound). Any future consumer of `project_id` must treat it as nullable.
- **BFF org-credential helpers** live in `web/src/lib/knowledge/app.ts` (`listOrgCredentials` / `createOrgCredential` / `revokeOrgCredential`), mirroring the project helpers minus the path project id. They hit S2's `GET/POST/DELETE /app/credentials`.
- **Server actions** `mintOrgCredentialAction` / `revokeOrgCredentialAction` were **appended to the existing** `web/src/app/(app)/dashboard/actions.ts` (which already held `createProjectAction`) — one `"use server"` dashboard module. Same rules as the project mint/revoke: `requireIdentity()` outside try, status-mapped errors, show-once key in action state, `revalidatePath("/dashboard", "page")`. Mint has no `notFound` case (org == caller tenant, always exists).
- **Client islands** are page-local copies (established pattern, no shared abstraction): `dashboard/mint-org-key-form.tsx` (`MintOrgKeyForm`, with `ShowOnceKey` inlined) + `dashboard/revoke-org-key-button.tsx` (`RevokeOrgKeyButton`, `credentialId`-only hidden input).
- **Copy Workspace→Org is label-only.** Renamed user-facing string VALUES across `content/{app,dashboard,auth,documents,graph,project}.ts`; **kept the code identifier `APP_SHELL.workspaceLabel`** (only its value moved to "Org"). Signup line → "A default org and project are created for you automatically." The **only** surviving `workspace` hits after a `grep -rni` sweep are code comments, that identifier, and `content/marketing/content.ts:93` — the **P20 marketing landing copy, deliberately left for P20**. S4's CLI/skill text should describe the same user-facing "org" naming for consistency.
- **New content exports** for the REVIEW/consumers: `DASHBOARD.orgKeys` (display copy) + `MINT_ORG_CREDENTIAL_ERRORS` / `REVOKE_ORG_CREDENTIAL_ERRORS` (barrel-re-exported from `@/content`).
- **Validation:** web typecheck / lint / `next build` all PASS; `vitest` 58/58 PASS; `plugin_parity` + `skills_parity` PASS (no-ops — web-only slice); `workflow validate` PASS. **Live mint→show-once→revoke click-through was NOT run** (needs a live Postgres + KB API + dev server) — build-verified-only; **S5's extended `onboarding_smoke.py` E2E is the live org-key proof** (same operator-residual shape as P16's in-browser round-trip).

### S4 landed (2026-07-22) — notes for S5 + REVIEW

- **`knowledge init` now mints an ORG key** via `client.credential_create_org()` → `POST /app/credentials` (`project_id NULL`, S2's endpoint), not the per-project `POST /app/projects/{id}/credentials`. **S5's extended `onboarding_smoke.py` must reflect this exact flow:** signup/login → ensure the `"default"` project exists (`_ensure_project`, still list-first + `POST /app/projects` create) → mint via `POST /app/credentials` → write config `{api:{base_url, token, project:"default"}, site:{...}, auth:{...}}`. The config's `api.project` is the **save fallback**, no longer a server-side key binding.
- **CLI outside-a-repo default is now `"default"`** — `auth.DEFAULT_PROJECT = "default"` (retargeted the shared constant, not a new one, per plan decision #1). This is `init --project`'s default AND `save`'s outside-a-repo fallback. `default_project()` chain = repo basename → `api.project` (config) → `"default"`. Since signup auto-provisions a `"default"` project (S1) and the write path get-or-creates (S2), `"default"` is always a real registry row.
- **Explain skill fallback ≠ CLI fallback.** The explain skill's config resolver reads only `kb_root`/`api.base_url`/`api.token`/`site.base_url` — it does **not** read `api.project`. So the explain skill's project rule (`SKILL.md` §5) falls back repo-name → `"default"` **only**; the fuller repo→`api.project`→`default` chain is the *CLI's* (`default_project()`), which does read `api.project`. Kept these two narrations deliberately distinct so neither lies.
- **Reuse-gate structure preserved (flagged for REVIEW).** `cmd_init`'s `same_project` gate keeps its shape (plan decision #2: "keeps its structure"), so `init --project other` mints a **second** org key even though one org key already serves every project. Mild tension with "one key, all repos" — a candidate follow-up (relax the gate to reuse an org key across projects, since org keys aren't project-bound), left un-done here as out of the plan's intent. Test `test_init_remints_when_the_configured_project_changes` pins the current behavior with an honest docstring.
- **Setup skill has no `.agents` mirror.** Only `explain` is duplicated into `.agents/skills/` (guarded by `skills_parity`); the setup skill is plugin-only, and skills aren't a `plugin_parity` shipped_dir — so setup-skill edits carry no cross-copy parity obligation. Connect-mode C1/C2/C3/C5 + the "one key, all repos" attribution + the CLI-path section were rewritten to the org model (see result.md); C3 now mints from the Dashboard **Org API keys** panel (S3's `/dashboard` surface).
- **No `server/**` or `tests/**` change this slice** → `plugin_parity` is a pure no-op (as DECOMP predicted). Only `cli/`, the two explain copies, and the setup skill changed.
- **Validation:** `cli` suite 39/39 PASS (`uv run pytest -q`); root `.venv/bin/python -m pytest -q` 70 passed / 19 Postgres-gated skips; `skills_parity` + `plugin_parity` PASS; `default_project()` outside-repo smoke → `"default"`; `workflow validate` PASS.

### S5 landed — Stage A (2026-07-22) — notes for Stage B + REVIEW

- **`scripts/onboarding_smoke.py` extended with the P18 org-model journey** (`_run_org_journey`, a new **section 4**). It runs in its **own fresh tenant C** (a second signup) so tenant B's existing `documents_created == 1` isolation/usage assertions stay pristine — this was a deliberate deviation from the plan's wording (which reads as one tenant) to honor "keep existing checks intact." Section 4 asserts: signup's additive `project` == `"default"` (visible via `/app/projects`); ONE org key (`POST /app/credentials`, `project_id: null`) writes to TWO never-pre-created project names → both appear in `/app/projects` (get-or-create) → per-named-project usage `documents_created == 1` each; `DELETE /app/credentials/{id}` → subsequent write **401**; a project-bound key still mints+writes. `scripts/` is **not** a `shipped_dir` → **no template mirror**, `plugin_parity` no-op.
- **Local proof is green.** Disposable `postgres:17` + `alembic upgrade head` (0001→0002→**0003**, constraint + columns exactly as designed) + `server.seed` + tenant-mode `uvicorn` → the extended smoke PASSes (exit 0), and the Postgres rows confirm the org key is `project_id NULL` + revoked, two projects get-or-created, usage attributed per named project. Stack torn down.
- **Prod pre-flight (read-only) — box is pre-P18; `0003` NOT applied; accounts DB migrated/live.** `GET /app/credentials` (unauth) → **404** (P18 org endpoint absent) while `GET /app/projects` → 401 (surface mounted) and `GET /app/documents/1/raw` → 401 (P16/P17 present); `POST /auth/login` nonsense → 401 "invalid email or password" (migrated/live discriminator). Git: `origin/main` = `84fc855` (P17 review), 7 behind local; `0003` + `create_org_credential` both ABSENT from `origin/main`. So the P10 fresh-DB `stop→migrate→seed→up` deadlock does **not** apply here (DB already migrated+seeded).
- **The cutover HAS a migration (unlike P17.S5), so ordering is load-bearing:** reconcile clone → one-shot `alembic upgrade head` (0003) → force-recreate api on new code, **back-to-back**. Deploying P18 code before 0003 breaks every `vk_` (resolver reads `credential.tenant_id`, the column 0003 adds). Safe overlap = old code + new schema; ⚠ old-code caveat: a **new key mint** or **duplicate-name project create** in the overlap window 500s (old code omits the now-NOT-NULL `tenant_id` / hits the new UNIQUE) — keep the window seconds-short. The migration is **manual** (Production Deploy runs no alembic), so P18 is an on-box manual sequence, not a bare Action dispatch. Full runbook + exact command forms in `slices/P18.S5/result.md`.
- **Stage A returned `needs_operator`;** the slice is `pending` on the operator running push → reconcile → migrate → deploy → verify. Stage B (hosted extended smoke) is a later dispatch.

### S5 landed — Stage B (2026-07-22) — notes for REVIEW

- **P18 accounts-v2 cutover is LIVE on `https://knowledge.hi2vi.com` and verified green.** The operator ran the §4 runbook (reconcile → `alembic upgrade head` 0003 → optional seed → `deploy/deploy.sh`); Stage B re-verified read-only and ran the hosted extended smoke.
- **Decisive P18-present flip confirmed:** unauth `GET /app/credentials` now → **401** (was **404** pre-cutover in Stage A) → S2's org-mint endpoint is mounted → P18 code is on the box. `GET /healthz` → 200 (both planes healthy). Login discriminator → 401 "invalid email or password" (accounts DB migrated + live).
- **Extended `onboarding_smoke.py` PASS (exit 0) against live prod, WITHOUT `--master-token`** (operator did not provide the master token — B-only isolation ran). One run, no retry-loop. Covers the tenant-B journey (isolation + `documents_created == 1` metering) **and** the section-4 org-model journey: signup default project, ONE org key → TWO get-or-create projects, per-named-project usage, revoke→401, project-bound regression — all green end-to-end in prod.
- **Login-probe methodology note (NOT a P18 change / NOT a defect):** the login discriminator needs a password ≥ 8 chars — `LoginIn` shares `_EmailPasswordInput` (`password: Field(min_length=8)`) with signup, and that constraint is **already present at P17 (`git show 84fc855:server/auth_api.py`)**. Password `"x"` 422s at request-validation before the credential check (Stage A's 401-with-`"x"` reflects the box then running older-than-84fc855 code). Took 2 throttle-aware login calls (of 20/900 s/IP) to reach the true 401. No contract concern — no real account has a <8-char password (signup enforces it), so no legitimate login is affected.
- **Residual (expected):** two throwaway tenants (B + org-journey C) with docs under `tenants/<uuid>/` — namespaced, isolated from tenant #1's public corpus (same shape as P16/P17). No delete API; operator may purge. No other prod mutations.
- **Slice P18.S5 is complete (both stages green).** No open follow-ups from Stage B for the REVIEW beyond the pre-existing S1-flagged gated `test_documents_api.py` `format`-key failure (unrelated to P18) and S4's flagged `init` reuse-gate re-mint (candidate follow-up, not a defect).

### Doc impact (running list — S4 appends below S3's entries)

- **experience** (S4) — CLI + explain/setup skill onboarding narration updated to the org model: `save` outside a repo → `"default"`; `knowledge init` mints an **org-level** key (`POST /app/credentials`); CLI labels read "org" not "workspace"; the guide names the org-level key + the `"default"` fallback; setup Connect mode's C1 says a default org + default project are auto-provisioned, and C3 mints from the Dashboard **Org API keys** panel.
- **product** (S4) — user-facing naming is "org" across the CLI + explain/setup skills; one org key serves every repo (made literal); projects are get-or-create (no pre-create step in any client flow).
- **decisions** (S4) — (a) retargeted `DEFAULT_PROJECT "knowledge"→"default"` (shared with `init --project`) rather than adding a separate save-fallback constant, since signup's default project makes the two coincide; (b) `init` mints org-level (`--new-key` too); (c) the `init` reuse-gate structure was preserved verbatim, so it re-mints on a recorded-project change even though org keys aren't project-bound (flagged as a candidate follow-up, not a defect); (d) the explain skill's fallback is repo→`"default"` only (its resolver ignores `api.project`), while the CLI keeps the fuller repo→`api.project`→`"default"` chain.
- **qa** (S4) — CLI `test_auth.py` adjusted (not expanded): `"default"` project assertions, a pin that `init` mints via the org endpoint and never the per-project one, and the renamed reuse-gate test. 39 CLI tests green.
- **operations** (S5) — P18 prod cutover runbook: this cutover carries a NEW migration (`0003`), so the order is load-bearing — **reconcile box clone → one-shot `alembic upgrade head` → force-recreate api on new code, back-to-back** (deploying P18 code before `0003` breaks every `vk_` auth; safe overlap = old code + new schema; ⚠ old-code mint/duplicate-project window 500s until the recreate — keep it seconds-short). The migration stays **manual** (Production Deploy runs no alembic), and the box's fresh-DB `stop→migrate→seed→up` deadlock does not apply (accounts DB already migrated+seeded+live). `scripts/onboarding_smoke.py` gains a section-4 org-model E2E leg (org key across two get-or-create projects, per-project usage, revoke→401, project-bound regression) — still the committed post-deploy verifier, not pytest.
- **qa** (S5) — `scripts/onboarding_smoke.py` extended with the P18 org-model journey (new `_run_org_journey` section, run in its own fresh tenant so the existing tenant-B isolation/usage checks stay intact); proven green against a disposable `postgres:17` tenant-mode stack (0003 + seed + uvicorn) with a Postgres row-level assertion of the org key (`project_id NULL` + revoked), the two get-or-create projects, and per-named-project usage attribution.
- **operations** (S5 Stage B) — the P18 accounts-v2 prod cutover was **executed and verified live** on `https://knowledge.hi2vi.com` (2026-07-22): the §4 runbook (reconcile → `alembic upgrade head` 0003 → deploy) landed, the decisive `GET /app/credentials` flip **404→401** confirms P18 code is serving, and the extended `onboarding_smoke.py` (B-only isolation + full org-model journey) passed green against the host — the committed post-deploy verifier now proves the org model in prod, not just locally.

## Constraints

- **Frozen public contract** `/api/*` + `/auth/*` + `/app/*` is **additive-only**. The org-level mint endpoint and any new fields are additions; `POST /api/documents`' consumer contract must be preserved (signup's response may gain fields, not lose them).
- **Single uvicorn worker** invariant: the write lock and in-process rate limiter depend on it — do not introduce anything requiring multiple workers or cross-process shared state.
- **Parity gates (CI `plugin-ci.yml`):** `plugin_parity` — `server/` + `tests/` (among the `shipped_dirs`) must match `plugin/templates/kb/` set-for-set and content-identical, mirrored **within the same slice**. `skills_parity` — `plugin/skills/explain/SKILL.md` ≡ `.agents/skills/explain/SKILL.md` (body-identical). `alembic/` is not shipped — migrations stay repo-only.
- **Prod schema changes are manual:** `alembic upgrade head` + `python -m server.seed` on the box, then `deploy/deploy.sh` — operator-run (S5 `pending` gate). No auto-migrate on boot for the control plane.
- **Postgres-gated tests:** accounts coverage runs only with `KB_TEST_DATABASE_URL` set, else skips; keep new tests terse (CLAUDE.md: minimal high-value cases, no fixture sprawl).
- **Scope boundaries (do not build here):** org **creation** + member **invites** / roles → deferred **D14**; **public/private** projects + direct doc URLs → **P19** (P19's visibility flag hangs off the project entity P18 reshapes); landing/hero/onboarding/skill-on-landing → **P20**. P18 stops at: default org+project on signup, org-level keys, get-or-create projects, CLI `--project`/`"default"` fallback.

## Doc impact expectations

Durable-truth areas each slice is expected to touch (append one-line "Doc impact" notes below as slices land; the REVIEW slice consolidates them into one new version per affected doc — never per slice). Anticipated across the phase:

- **product** — accounts model becomes user → org → project; signup gives a default org + default project; org-level keys. (S1, S3 wording)
- **backend** — signup provisioning primitive; resolver reads credential `tenant_id`; write-path get-or-create; metering alignment. (S1, S2)
- **api** — additive org-level mint/list endpoint(s); signup response shape; `POST /api/documents` get-or-create side effect; contract stays frozen/additive. (S1, S2)
- **data** — `projects UNIQUE(tenant_id,name)`; `project_credentials.tenant_id` + nullable `project_id`; alembic 0003; org-level (`project_id NULL`) credential rows. (S1, S2)
- **architecture** — user→org→project model; the tenant-row-is-org decision; org-level-key resolution seam. (S1, S2)
- **decisions** — no tenants→orgs rename (stance #1); org keys additive vs. rename; get-or-create + UNIQUE choice; CLI save-fallback constant vs. retargeting `DEFAULT_PROJECT`; no Claude Design round for the org-keys surface. (all slices — record rationale)
- **frontend** — org-level keys surface (reused components) + workspace→org copy; new BFF routes. (S3)
- **experience** — signup UX (auto default org/project), org-keys journey, CLI `--project`/`"default"` fallback flow. (S3, S4)
- **operations** — prod alembic 0003 + seed + deploy runbook; extended `onboarding_smoke.py` E2E steps. (S5)
- **security** — org-level key authorization semantics (one vk_ grants the whole org, made honest); credential resolution unchanged in trust boundary. (S2 — confirm whether the trust boundary shifts; likely a wording update since enforcement was already tenant-wide)
- **qa** — extended onboarding E2E + new Postgres-gated accounts tests. (S1, S2, S5)

(These are expectations, not commitments — the review consolidates the actual "Doc impact" notes accumulated below.)

### Doc impact (running list — slices append; REVIEW consolidates)

- **data** (S1) — alembic `0003`: `projects UNIQUE(tenant_id, name)` (with defensive de-dupe of pre-existing dupes, oldest-wins); `project_credentials.tenant_id` NOT NULL (FK tenants CASCADE, indexed) backfilled from the bound project's tenant; `project_credentials.project_id` now nullable (org-level credential rows will carry `project_id NULL`). ORM models mirror this.
- **backend** (S1) — new `provision_signup` accounts-service primitive (default org + default project + owner membership, atomic); signup + fresh-DB seed both build on it; `create_tenant_with_owner` retained for bare-tenant callers. Seed no longer renames existing tenants.
- **api** (S1) — `/auth/signup` response gained an **additive** `project` field alongside `tenant` (frozen `/auth/*` contract preserved — additive only). The `/app/*` mint path now records `tenant_id` on each credential (no contract change).
- **architecture** (S1) — credentials are now tenant-scoped at the schema level (`tenant_id` always set); the split between org-level (`project_id NULL`) and project-bound credentials is introduced structurally, ready for S2's resolver/mint to use it.
- **decisions** (S1) — (a) `serialize_project` kept in `app_api` (canonical) with a byte-identical local copy in `auth_api` rather than moved, to avoid an `app_api → auth_api` import cycle; (b) `CreateProjectCredential`/`ProjectCredentialRecord.project_id` made `UUID | None` in S1 so S2 needs no dataclass rework; (c) destructive-by-design downgrade for `0003` (fix-forward repo); (d) no alembic-running pytest shipped (parity: `alembic/` is repo-only) — migration validated manually against a disposable Postgres.
- **qa** (S1) — new Postgres-gated `test_accounts_provisioning.py` (signup default org/project + minted-credential `tenant_id`); `test_dashboard_api.py` updated for the auto-provisioned default project. Surfaced a pre-existing, unrelated gated failure in `test_documents_api.py` (documents-plane `format` key) — flagged, not owned by P18.
- **api** (S2) — additive org-level credential endpoints on the frozen `/app/*` contract: `POST /app/credentials` (mint org key, `project_id NULL`, show-once `{credential, key}`), `GET /app/credentials` (org-level list), `DELETE /app/credentials/{credential_id}` (idempotent revoke, 404 anti-probe). `serialize_credential.project_id` is now nullable (`null` for org keys). `POST /app/projects` is now get-or-create (dupe name → 201 existing row, no longer a 500). `POST /api/documents` gains a get-or-create side effect (an unregistered `body.project` now leaves a `projects` row); consumer contract unchanged.
- **backend** (S2) — resolver (`api_auth._resolve_tenant_bearer`) reads `cred.tenant_id` directly: org keys (`project_id NULL`) authorize at tenant scope with no project query; project-bound keys keep the vanished-project existence guard. New `get_or_create_project` (race-safe on `UNIQUE(tenant_id, name)`) + `list_org_credentials` service/repository methods. Write path gets an async `ensure_registry_project` dependency in front of the sync handler (FastAPI shares the body + caches the resolver). `metering.py` unchanged (name-first lookup now always resolves).
- **architecture** (S2) — org-level-key resolution seam realized: every `vk_` resolves via its own `tenant_id`; org vs project-bound is a `project_id NULL` distinction the hot auth path now honors. Get-or-create sits in an async FastAPI dependency ahead of the sync, `WRITE_LOCK`-holding write handler — the seam that lets a Postgres write precede a sqlite critical section.
- **security** (S2) — org-level key authorization made honest: one `vk_` grants the whole org. **No trust-boundary shift** — enforcement was already tenant-wide (resolver returned `ctx.tenant_id`; the write path never checked `body.project` against the credential). Wording update only. Revoked org keys resolve to nothing → 401, same as project-bound.
- **decisions** (S2) — (a) resolver returns `tenant_id=cred.tenant_id` for both org and bound keys (S1 backfilled it = `project.tenant_id`), keeping the existence guard **only** where `project_id` is set; (b) get-or-create is race-safe by catch-`IntegrityError`-and-re-read against the new UNIQUE rather than an upsert; (c) write-path get-or-create lives in an async dependency (not inside `record_usage`) so the row exists even for an unmetered save, and row-before-409/422 is accepted as idempotent; (d) `POST /app/projects` folded into get-or-create to also close S1's latent dupe-500.
- **qa** (S2) — new Postgres-gated `tests/test_org_credentials.py` (org mint + resolve + get-or-create proof, project-bound regression, revoked→401, dupe-project idempotency). Requires a Postgres DSN **and** an on-disk `KB_ROOT`.
- **frontend** (S3) — `web/` gains an **Org API keys** panel: a full-width `kb-panel` on `/dashboard` (below the projects/activity grid, no new route/rail change) reusing the P12 keys-table + mint-modal + revoke components at org scope (page-local `MintOrgKeyForm` / `RevokeOrgKeyButton` islands + the two org actions on `dashboard/actions.ts`). New BFF helpers `listOrgCredentials`/`createOrgCredential`/`revokeOrgCredential` (`lib/knowledge/app.ts`) hit `GET/POST/DELETE /app/credentials`. `KbCredential.project_id` widened to `string | null` (org keys serialize null). Product-facing "Workspace" copy renamed to "Org" across the app content (labels only; CSS classes + code identifiers unchanged). **No Claude Design round** — same visual language at org scope (stance #6).
- **experience** (S3) — signup copy now says a **default org + default project** are created automatically (was "A workspace is created for you automatically"); the app chrome/eyebrows read "Org" instead of "Workspace"; users mint/list/revoke an **org-level** `vk_` (one key → whole org) from the dashboard, alongside the unchanged per-project keys on the project page.
- **decisions** (S3) — (a) org-keys surface reuses P12 components at org scope → **no Claude Design round** (stance #6 upheld in code, no new IA); (b) placement = a dashboard panel, **not** a dedicated org-settings route (no rail/route change); (c) Workspace→Org is **label-only** — the tenant row stays the org (stance #1), no CSS class or code identifier renamed (the `workspaceLabel` key is kept, value only moved); (d) `KbCredential.project_id` widened to `string | null` (honest to S2's NULL-safe serializer) rather than a separate org-credential type — one shared credential shape; (e) org mint/revoke actions appended to the existing `dashboard/actions.ts` and client islands kept as page-local copies (no shared abstraction), matching the established P12 convention.

## Open Questions

Resolved during decomposition (no open blockers):

- **Split shape?** → 5 middle slices (S1 schema+provisioning, S2 org-keys behavior, S3 web, S4 CLI/skills, S5 prod+E2E). Backend before clients; clients (S3/S4) parallel-safe on S2; prod isolated last.
- **Rename tenants→orgs?** → No (design stance #1) — tenant row is the org; product naming only.
- **Org-key schema shape?** → Additive: `project_credentials.tenant_id` backfilled + `project_id` nullable; org rows carry `project_id NULL`.
- **CLI `--project`?** → Already exists (P13); only the outside-a-repo fallback changes to `"default"` (S4 decides constant vs. `DEFAULT_PROJECT` retarget).
- **New design surface for org keys?** → No Claude Design round (reuses existing P12 components at org scope); S3 flags if it disagrees.
- **Deferred as noted:** org creation + invites → D14 (out of scope).
