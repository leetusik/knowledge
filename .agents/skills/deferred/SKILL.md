---
name: deferred
description: Rebuild and show the deferred jobs dashboard.
---

# deferred

Run `python3 scripts/workflow.py deferred`. Then read `works/deferred.md` if you need the human-readable dashboard.
