interface:
  display_name: "commit"
  short_description: "Group pending changes by topic into focused conventional commits."
  default_prompt: "Use the commit skill."
policy:
  allow_implicit_invocation: false
